{
    "formatVersion": 1,
    "id": "illumina-amplicon-merge",
    "name": "Illumina Amplicon Merge",
    "description": "Prepare paired-end Illumina amplicon reads for exact-match genotyping by keeping only overlap-merged synthetic reads. The normal FASTQ import finalization step then clumpifies and compresses each sample bundle.",
    "author": "Lungfish Built-in",
    "tags": ["illumina", "amplicon", "paired-end", "genotyping", "merge"],
    "platforms": ["illumina"],
    "requiredInput": "paired",
    "qualityBinning": "illumina4",
    "steps": [
        {
            "type": "fastp-merge",
            "label": "Merge overlapping pairs",
            "params": {
                "minOverlap": 20,
                "keepUnmerged": false
            }
        }
    ]
}
