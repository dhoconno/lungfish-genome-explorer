# Dependency Upgrade Program: Plan C (Sweep Tooling + 2026.2 Sweep) Results

Date: 2026-08-18. Branch: claude/lge-dependency-upgrade-plan-b6b53b (base ca433879 = deps-plan-b-complete, head 4c4c14a6). Tag: deps-plan-c-complete.

Companion documents:

- Sweep results by group (baseline, defects, groups 1 to 3, held builds, goldens, SwiftPM, final fix wave): docs/reports/2026-08-18-dependency-sweep-2026.2-results.md
- Release notes for dependency set 2026.2: docs/release-notes/deps-2026.2.md
- Sweep checklist (updated with what this sweep proved missing): docs/release/dependency-sweep.md
- Plan A results: docs/reports/2026-08-18-dependency-upgrade-plan-a-results.md; Plan B results: docs/reports/2026-08-18-dependency-upgrade-plan-b-results.md

## Summary

Plan C delivered the sweep tooling (`scripts/deps/check-upstream.py`, `bump.py`, `manifest_io.py`, `upstream_sources.py`, `verify.sh`) and ran the first real sweep, moving the manifest from 2026.1 to 2026.2. Thirty-one manifest entries moved (17 spec changes, 5 build-only changes, 1 metadata mismatch on a 2026.1 machine plan, plus Sparkle, micromamba, and the SwiftPM minor updates). Held with reasons: ncbi-taxonomy (zip archive), bracken and blast (no arm64 build), sra-tools (build-only, no ABI tag), gatk4 and whatshap (experimental), viralrecon, the EsViritu database, the deacon indexes, containerization.

Product defects the sweep surfaced and fixed: Bracken on Apple Silicon (dialect detection, launcher attribution, `-l D` degrades), kmer_distrib nearest-read-length substitution surfaced in the result and provenance, database updates for rows registered from disk (`resolveUpdateTarget`, then the shared `MetagenomicsDatabaseInfo.resolvedCatalogEntry` used by the reconciler, `availableUpdateVersion`, the CLI, and the new Plugin Manager Update button), `db update --all` false success, empty-VCF import indexes, ordered flagstat categories, injected home for `CoreToolLocator`, `ensureMicromamba` installed fallback.

Verification: verify.sh tiers 1 and 2 in the isolated root `~/.lungfish-verify` (tier 1 189/0/0 after each group; tier 2 all same at 2026.2 after the megahit recipe was made deterministic). Full gate on 3c6859c2: 13433 XCTest executed, 36 skipped, 0 failures; swift-testing issues limited to FileSystemWatcherTests (environmental). Final gate on the fix-wave head recorded below.

## Open items (user-gated or environment-gated)

1. GUI walkthroughs: DONE 2026-08-19 on a fresh isolated root (Update Tools sheet with the 2026.2 pins and the `catalogID: nil` Viral row planned; Plugin Manager Update button driving a real download and the locally-built notice inline; Operations panel lifecycle). Record: `docs/verification/2026-08-18-update-tools-sheet.md`. Still unrun: the Update button disabled while a reconciliation is applying, an update carried to completion with the receipt written, and the upgraded 2026.1 root in the GUI (its plan was verified through the CLI).
2. CI `toolset-conformance` dispatch needs the branch pushed (`gh workflow run ci.yml --ref claude/lge-dependency-upgrade-plan-b6b53b`).
3. Tier 3 (`run-pipelines.sh`) needs Apple Containers or Docker on the host.
4. savont 0.6.3 end-to-end: an ONT fixture is now pinned (SRR31764993, fetched by `scripts/deps/fetch-savont-fixture.sh`) and the integration test picks it up automatically. Remaining gap: a primate MHC amplicon on R10.4 or newer chemistry, since the pinned fixture is 16S.
5. Kraken2 index integrity: the S3 bucket publishes no checksum sidecars; follow-up is multipart-ETag verification.
6. ncbi-taxonomy zip support; bracken-build in a container as a candidate for special databases.

## Final gate (head 4c4c14a6)

Full `swift test --skip-update` on 7cdbd160 (fix wave + resolver tie-break): 13447 XCTest executed, 36 skipped, 2 failures; swift-testing 607 tests, 2 issues.

- swift-testing: both issues are FileSystemWatcherTests (FSEvents callbacks never fire on this machine; known environmental, fails on main too).
- `PluginPackStatusServiceTests.testStatusForPackReusesPerPackCachedResultWithinTTL`: latent order-dependent assertion (requirements are evaluated in a concurrent task group since fe07c233); four sites made order-independent in the follow-up commit. Not a branch regression.
- `GATKPipelineExecutorTests.testProcessRunnerTerminatesProcessPromptlyWhenTaskIsCancelled`: pid file empty under load; passes in isolation and passed on the 3c6859c2 gate. Timing flake, unchanged on this branch.
- Isolated rerun of both suites after the test fix: 42 executed, 0 failures.

Verdict: GREEN modulo the known environmental set.

## Execution ledger

Ledger of docs/superpowers/plans/2026-08-17-dependency-upgrade-03-sweep.md, kept verbatim.

```
# SDD ledger — plan: docs/superpowers/plans/2026-08-17-dependency-upgrade-03-sweep.md

Spec: docs/superpowers/specs/2026-08-17-dependency-upgrade-mechanism-design.md (sections 4.6, 5, 6)
Branch: claude/lge-dependency-upgrade-plan-b6b53b. Start HEAD: 74e33b0a (Plan A + Plan B complete on branch)
Prior ledgers: docs/reports/2026-08-18-dependency-upgrade-plan-a-results.md; .superpowers/sdd/2026-08-17-dependency-upgrade-02-regression/progress.md (to be copied to docs/reports)

## Pre-flight scan

| Tasks | Produces vs consumes | Finding |
|---|---|---|
| C1↔C2 | candidates.json schema (`tools[]{id,kind,status,latest,latestSpec}`, `pipelines[]{id,latest,latestSha}`, `databases[]{id,latest,latestURL,latestFilename}`, `bootstrap{latest}`) | consistent between the two test files |
| C2 | manifest_io must round-trip the CURRENT manifest byte-for-byte; manifest now has extra sections/keys (`retiredEnvironments`, `bootstrap.sha256` pinned, `packTools` one-line style?) | implementer must inspect current formatting; ruling: match whatever the file is now |
| C3↔B1/B10 | verify.sh uses `full-suite-gate.sh --require-tools --filter` and CLI (`tools update --apply --yes --required-only`, `conda install --pack X` (no --yes), `conda db download Viral` (no --yes), new `conda db install-managed deacon-panhuman`) | brief's flags outdated → ruling: use real flags |
| C4 baseline | ToolVersionConformanceTests require-mode fails on this machine (bwa-mem2 2.2.1 vs 2.3) until the isolated verify root is provisioned from the manifest | verify.sh uses an isolated LUNGFISH_STORAGE_ROOT so drift is reconciled there |
| C5-C7 | bump lists reference `--only` ids like `read-mapping/minimap2`; C1 candidate ids must use `packID/toolID` for pack tools | consistent with C2 test |
| C5 | bracken: manifest pins 1.0.0 but bioconda has 2.x/3.x; check-upstream must surface the real latest and the sweep must decide (Plan B found installed 3.0.1) | note for C5 ruling |
| C5 | taxonomy dated archive is a .zip; MetagenomicsDatabaseInstaller archive step may not handle zip | brief says add unzip support + test if needed |
| C9 | goldens for 2026.2 regenerate via B8 tooling; kraken2 catalog changes DB (viral 20260626) → kraken2-mini golden will change; justification recorded | expected |
| Global | run only ONE swift invocation at a time; verify.sh calls swift build/test; never run verify while another swift runs | ok |

Rulings (pre-flight):
- Ruling: execution order C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12 as planned; C1/C2 (python) may run while a swift gate is running; C3+ serialize behind swift.
- Ruling: model routing — C1,C2 opus (parsing/format logic); C3 sonnet; C4-C12 opus (verification judgment) with Fable owning verdicts.
- Ruling: CI dispatch (C11 step 3) requires pushing the branch to origin — pushing a feature branch is a shared-remote side effect: STOP and ask the user before pushing; record as open item if not granted.
- Ruling: GUI walkthroughs (C11 steps 1-2) still blocked on Computer Use access; park as open items if access is still denied.

## Task execution order
C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12

## Progress
Task C1: implementer DONE at b17d9a87. Findings: bracken >=2.2 and blast 2.17.0 have no osx-arm64 build → both HELD (spec table corrected); aws-indexes page stale for size-capped collections (S3 probing used); NCBI FTP flaky (retry). Ruling: bracken/blast excluded from bumps; build-only churn (pysam py310→py39 etc.) is opt-in via --only.
Task C1: review Needs fixes (version_key padding: 1.24 vs 1.24.0 unequal) → fix round 1/5 dispatched at b17d9a87
Task C1: fix round 1/5 (2 addressed, 0 open; commits b17d9a87..37a4ccc3)
Task C1: complete (commits 74e33b0a..37a4ccc3, review clean). Ruling: conda -N build suffix compares as an ordinary numeric segment (2.0.5-0 == 2.0.5); post-release suffixes (p1) outrank the bare release.
Task C2: implementer DONE at 6864448f. Notes for C5+: hold ncbi-taxonomy until zip archive support is confirmed; build-only moves cross Python ABIs (pysam py310→py39 backwards!) — treat pysam/openpyxl held-build moves as opt-in and verify; sra-tools build string odd.
Task C2: complete (commits 37a4ccc3..6864448f, review clean)
Task C2: minor (deferred): no test for --hold/--only against bootstrap; notices table width cosmetic.
Task C3+C4: dispatched (opus); agent interrupted by API 529 mid tier-1 baseline; resumed. Ruling: verify.sh --seed-from clones an existing root (APFS cp -Rc) so local runs only reconcile drift — cost if wrong: local verify roots inherit stale pkgs cache (harmless).
Task C3: complete (commit 438f9eef + dd53f49b fix).
Task C4: complete (commit 4e328afc). Tier 2 PASS (13 same, 42.6s). Tier 1 RED at 2026.1 for TWO GENUINE PRODUCT/UPSTREAM DEFECTS (controller-verified directly, not harness bugs):
  (a) bwa-mem2 pinned 2.3=hda5e58c_0 self-reports "2.2.1" (upstream packaging defect; conda-meta says 2.3). No newer arm64 build exists.
  (b) bracken pinned 1.0.0=1 (the ONLY arm64-installable build; 2.x/3.x are linux-only) ships a wrapper `exec python est_abundance.py "$@"`, so the production invocation in ClassificationPipeline.swift:1360-1367 (`bracken -d/-i/-o/-r/-l/-t`) fails with "arguments are required: -k/--kmer_distr". This is a REAL user-facing break of Bracken abundance estimation on Apple Silicon, surfaced by Plan B conformance — exactly what the program was built to catch.
Ruling: (a) bwa-mem2 → record a version-report exception in the conformance table (assert the conda-meta version, not the self-reported string) with a comment naming the upstream defect; do NOT weaken other tools. (b) bracken → fix in the product: invoke the real entry point (`est_abundance.py` with `-k <db>/database<readlen>mers.kmer_distrib`) when the wrapper is the broken passthrough, or call `python est_abundance.py` directly with the resolved kmer_distrib; keep provenance/tool naming as "bracken". Cost if wrong: a narrower code path than upstream's wrapper; mitigated by the conformance test.
Task C4 defect fixes: commits 74d42c26 (bracken CLI-dialect detection + inner entry point) and 8403ff22 (conda-meta version exceptions for bracken/bwa-mem2; results doc). Tier 1 re-run: GATE PASS 189/0/0. Extra findings: est_abundance.py UnboundLocalError on kreports without a U line (tolerated by exact signature, output validated); bracken wrapper has no version flag (version from conda-meta); bracken-build absent on arm64 → Kraken2 special DB builds unavailable on Apple Silicon (fails cleanly). Ruling: containerized bracken for abundance rejected (I/O-bound over the DB, no arm64 images, second pinning surface); containerized bracken-build for special DBs is a candidate follow-up AFTER the sweep. Pre-existing: 5 MetagenomicsDatabaseRegistryTests fail under LUNGFISH_STORAGE_ROOT (env overrides temp roots) — follow-up.
Task C4: complete.
Review C3/C4+defect fixes: Approved with 3 Important (wrapper is Lungfish's own ensureBrackenLauncher, package ships no driver — docs/comments misattribute; -l D unsupported in wrapper dialect → map to K; results-doc evidence mismatch) → fix round 1/5 dispatched. Minors: verify.sh registry rewrite path normalization; plan_status collapses to 65; --keep no-op; crash-signature substring match.
Task C4: KEY FACT for C5-C8: bracken cannot be fixed by re-pinning (no arm64 driver in any bioconda build); the product-side dialect fix is the durable solution.
Defect fixes round 1: commit 189da463. Ruling: -l D is NOT mapped to K (distinct ranks; would silently profile Orthornavirae for Viruses); D degrades via .unsupportedRank with a diagnostic — cost if wrong: CLI users get a clear failure for domain-level bracken on arm64 instead of a wrong answer. Evidence mismatch did not occur (real root has a genuine bracken.sh driver).
Defect fixes: fix round 1/5 (3 addressed, 0 open; commits 8403ff22..189da463). C3+C4 complete with defect fixes; tier 1 baseline GREEN at 2026.1.
Task C5: BLOCKED (correctly) — bump.py fetch_checksums ignored --only/--hold and read micromamba digest from releases/latest → held 2.0.5-0 pin would carry 2.9.0-0's sha256 (supply-chain relevant); rolling ncbi-taxonomy gained an md5. Findings doc committed e4e5695f; manifest untouched. Fix dispatched to C2 owner. Ruling: ncbi-taxonomy HELD this sweep (dated archive is .zip; extraction spans two files + provenance contract; dedicated follow-up task) — cost if wrong: taxonomy stays on the rolling URL one more sweep.
bump.py checksum fix: commit 78ad27e8, re-review clean. C5 resumed at 78ad27e8.
Task C5: resumed after watchdog stall; bump applied (uncommitted at that point), tier 1 PASS 189/0/0 on the bumped manifest; continuing tier 2 + DB update + tier 3.
Task C5: complete (commits b59126ab bumps + Sparkle 2.9.6; cbd8c797 doc). Tier 1 PASS 189/0/0. Tier 2: 12 same, 1 different (deacon 0.16.0 adds `check_pairs` key; numerics identical) → Ruling: accepted, additive; 2026.2 goldens carry it (C9). DB update: human-scrubber applied+old removed. Tier 3 BLOCKED (Apple Containers absent + Docker unreachable on this machine; run-pipelines.sh ignores LUNGFISH_STORAGE_ROOT).
Task C5: PRODUCT DEFECT (pre-existing): installed metagenomics rows registered via registerExisting have catalogID nil → MetagenomicsDatabaseRegistry.updateDatabase filter never matches; `conda db update --all --yes` prints "No databases have an update available" exit 0 while `db list` advertises one (silent false success). Ruling: fix now — resolve the installed row by catalogID OR the same (name, tool) match availableUpdateVersion uses; --all must apply advertised updates or exit non-zero with a clear message when none is applicable. Also: verify.sh plan gate ignores advisory-only DB updates; run-pipelines.sh honors LUNGFISH_STORAGE_ROOT; bump.py refreshes releaseDate for date-versioned DBs.
C5 fixes: commits 971a3ea7 (DB update matches registerExisting rows; --all false-success fixed; checksum lookup keyed correctly), 9f78ae7d (verify.sh advisory-only gate; run-pipelines honors root), 2e738f88 (bump.py releaseDate), 59d5b4a3 (conformance taxid match + doc). Live: viral DB updated to 20260626 in verify root; old removed. RELEASE-NOTES ITEM: 20260626 index renames SARS-CoV-2 species to Betacoronavirus pandemicum (taxid 3418604; 2697049 demoted to S1; old index used 694009) — review name-keyed display surfaces before shipping.
C5 fixes review: Needs fixes (network-dependent CLI tests in default suite; --all exit 0 when only locally-built DBs advertised) → fix round 1/5 dispatched at 59d5b4a3
C5 fixes: fix round 1/5 (3 addressed, 0 open; commits 59d5b4a3..6a557612). Group 1 fully landed. Open (env): tier 3 blocked (no Apple Containers/Docker here). Release-notes item: SARS-CoV-2 species rename in Kraken2 20260626.
Task C6: complete (commits aaf492fa bumps + bb73b8ce doc). Four ids: samtools/bcftools/htslib 1.23.1->1.24, spades 4.2.0->4.3.0; semantic manifest diff confirms only those four moved. Tier 1 GATE PASS 189/0/0. Tier 2 vs 2026.1 goldens: 12 same, 1 different (deacon check_pairs, group 1's accepted additive key) — ZERO differences attributable to group 2: flagstat/idxstats/minimap2-flagstat/bcftools calls.vcf/spades contigs-stats all byte-identical; bcftools call defaults unchanged (12 records, headers stripped); SPAdes 4.3.0 identical (32 contigs, N50 307) so the 5 percent tolerance was unused. Live: samtools/bcftools/bgzip/tabix all 1.24 linking htslib 1.24, spades.py 4.3.0, flagstat -O json parses byte-identical.
Task C6: fixed 4 STALE TEST MIRRORS LEFT BY C5 (verified failing at HEAD pre-bump by stashing): PluginPackRegistryTests nextflow 25.10.4->26.04.6, DatabaseRegistryTests + HumanScrubberDatabaseTests human_filter.db.20250916v2->20260706v2 (filename/date/URL/md5URL), plus this group's bcftools 1.23.1->1.24. Ruling: NoLiteralDependencyPinsTests false-positived on COMMENT prose naming bioconda::bracken=1.0.0 in ClassificationPipeline.swift + BrackenInvocationForm.swift (C4's defect documentation); guard now strips // lines rather than allowlisting two whole files — cost if wrong: a real pin hidden inside a comment goes unflagged, versus blinding the guard to those two files entirely.
Task C6: OPEN (env, group 1's pin not this group's): ToolVersionConformanceTests.testEveryManifestToolReportsPinnedVersion fails in a bare `swift test` (`nextflow: expected 26.04.6`) because a bare run resolves against the real ~/.lungfish; passes in the verify root after tier 1 reconciles. Default suite is red on this machine until the real root updates or the test skips on an unreconciled root — Fable to decide.
Task C6: PROCESS NOTE for C7/C8: read BOTH halves of swift test output. C5 reported "192 swift-testing tests, 0 failures, no mirrors needed fixing" while 4 XCTest mirrors were already failing; the swift-testing summary line alone hides XCTest failures. Also: verify.sh tail prints "GATE PASS ... Executed 2 tests" (last swift-testing suite), not the total — grep the gate log. Also: the manifest of record is ManagedTools/third-party-tools-lock.json; Tools/tool-versions.json is the micromamba bootstrap file and shows only a timestamp change.
Task C6: complete (commits aaf492fa bumps, bb73b8ce doc). Tier 1 PASS 189/0/0; tier 2: zero group-2 differences (deacon check_pairs only). Fixed 4 stale test mirrors from group 1 (nextflow, human-scrubber) + NoLiteral guard now ignores comment lines. Ruling: version drift (required or pack) is XCTSkip in default mode, XCTFail in require mode — dev default gate stays green mid-sweep; verify/CI enforce — cost if wrong: drift on dev machines only visible via verify.sh (by design). Follow-up dispatched pre-review.
Task C6 addendum (coordinator ruling on concern 1): commit 56b60661. ToolVersionConformanceTests — BOTH version tests now XCTSkip on VERSION DRIFT when requireTools is false and XCTFail when true; missing tools keep existing semantics. Only testEveryManifestToolReportsPinnedVersion needed changing (pack-tool test already had the shape): added drifted list + recordDrift helper at both mismatch sites incl. the savont --version/--help fallback. Doc comment states both modes + rationale (default gate/pre-push must stay green while the real root lags the manifest mid-sweep; verify.sh and CI enforce in require mode, so this relaxes WHERE not WHETHER). Verified: default mode 4 executed/2 skipped/0 failures naming nextflow 26.04.6 AND minimap2 2.31 (a second real-root lag the old hard failure had masked); require mode + verify root 4/4 pass with ZERO skips (enforcement path intact).
Task C6 addendum: FLAKE NOTED (unrelated, not a regression): AssemblyResultViewControllerTests.testRerunBlastButtonReRunsBlastForCurrentSelection "Timed out waiting for condition" under full-suite load; passes in isolation 2/2 in ~0.3s against a 5.3s-under-load timeout; touches no dependency pin. Candidate follow-up: raise its wait bound.
Task C6: complete (commits 6a557612..56b60661, review clean). Minor (deferred): NoLiteral guard is line-prefix comment-aware only (no /* */); AssemblyResultViewControllerTests.testRerunBlastButtonReRunsBlastForCurrentSelection load-timing flake (candidate: raise wait bound).
Task C7: complete (commits 84d457a2 bbmap 40.02, 608a13b8 savont 0.6.3, 18591b89 micromamba 2.9.0). Semantic manifest diffs confirm only the intended fields moved each time. Tier 1 GATE PASS 189/0/0 after every bump; tier 2 vs 2026.1 goldens 12 same / 1 different (group 1's accepted deacon check_pairs) = ZERO differences attributable to group 3. bbmap: JRE UNCHANGED (openjdk 25.0.2, lib/jvm/bin/java still resolves) and bbtools stderr is NOT a parsing surface (exit-status + provenance envelope only) — fixed 1 stale mirror (FASTQIngestionPipelineTests literal 39.80 now reads ManagedToolLock). savont: ALL 7 flags survive 0.6.3 (asv, -o, -t, --quality-value-cutoff, --min-cluster-size, --min-read-length, --max-read-length, --single-strand) so NO pipeline change needed; but DEFAULTS changed (cutoff 98, min-cluster-size 12, min-read-length 1100, max-read-length 2000) + new subcommands — Lungfish passes all of these explicitly so behaviour is unchanged. micromamba: digest verified twice (bump.py refetch == direct curl|shasum, ec2a072f...); fresh root seeded from a 2.0.5 root logged "bootstrap micromamba 2.0.5 -> 2.9.0-0", reports 2.9.0, empty plan after, tier 1 189/0/0.
Task C7: NEW TEST SavontCLIContractTests (Tests/LungfishWorkflowTests/Conformance/) — runs `savont asv --help` and asserts every option token the production argument builder emits is still documented; flag list DERIVED from SavontClusteringRunRequest.arguments (not restated) so new flags are covered automatically. Verified it FAILS on a simulated rename/removal and does not false-match --min-read-length inside --max-read-length or -o inside --output-dir. Ruling: pin the flag vocabulary, not just the version — cost if wrong: one more live-tool test to maintain, versus a renamed flag surfacing only on user data at genotyping time.
Task C7: OPEN (needs user data): real full-length ONT MHC genotyping run = MANUAL VERIFICATION PENDING (SavontClusteringIntegrationTests skips without an env var pointing at a real ONT FASTQ). Recorded in the results doc.
Task C8: complete (commit 4a3d35cf). cutadapt TAKEN (py311hd78823b_1 -> py313hf513372_2, interpreter forward; invoked as a binary, not imported by Lungfish Python; live check cutadapt 5.2 on Python 3.13.15). pysam HELD at py310hf7cbfa5_0. Tier 1 filtered GATE PASS 115/0/0.
Task C8: KEY FINDING for future sweeps: check-upstream.py picks the newest build BY TIMESTAMP with NO notion of Python ABI direction, so for multi-interpreter rebuilds it can recommend an interpreter DOWNGRADE while labelling it "newer build". It did exactly that for pysam (py310 -> py39). bioconda DOES publish py310hf7cbfa5_1 — the same-ABI rebuild at the newer build number — which is the move the hold is really waiting on. Ruling: hold pysam this sweep; taking the same-ABI build requires check-upstream.py to prefer same-or-forward ABI over newest timestamp (a tooling change, not a pin edit) — cost if wrong: pysam stays one build number behind on an interpreter that still works. Of the other build-only rows only pysam regresses; openpyxl/lofreq/flye move forward and were out of scope.
Task C7+C8: docs appended to docs/reports/2026-08-18-dependency-sweep-2026.2-results.md (commit 31013572, no em dashes); full report at .superpowers/sdd/2026-08-17-dependency-upgrade-03-sweep/task-C7-C8-report.md. All holds listed with reasons. Tier 3 still blocked (no Apple Containers/Docker). ~/.lungfish never touched; all swift invocations serialized.
Task C7: complete (84d457a2 bbmap 40.02; 608a13b8 savont 0.6.3 + SavontCLIContractTests; 18591b89 micromamba 2.9.0 with verified digest ec2a072f… + fresh-root proof). Tier 1 189/0/0 after each; tier 2 zero group-3 differences. savont end-to-end on real ONT data: MANUAL VERIFICATION PENDING (user data).
Task C8: complete (4a3d35cf cutadapt newer build; pysam HELD — check-upstream proposed a py310→py39 ABI downgrade; bioconda has py310hf7cbfa5_1). Ruling: fix check-upstream ABI awareness (prefer same-or-newer pyXY tag as the current pin; then highest build_number), then take pysam py310 _1 — cost if wrong: pysam stays on _0 (harmless).
C7/C8 review: Approved w/ 1 Important (stale SHA in results doc) → fix round 1/5 dispatched incl. ABI-aware build selection + pysam py310 _1.
C7/C8 fix round 1: complete (commits 6645040a pysam+tooling, 745c5332 docs). (1) Doc SHA corrected: savont was cited as 653f93f5 (the PRE-AMEND object, unreachable from branch — I amended that commit to restore shell-eaten backtick text); branch commit is 608a13b8. ALL other SHAs in the doc verified against git log and resolve. (2) ABI-aware selection implemented in upstream_sources.py: python_abi() returns (major,minor) tuples (py310/py39/py3_10); latest_conda(current_build) discards lower-ABI candidates, highest build_number wins; lower-ABI-only => status "same" + note "newer build only on lower Python ABI: <spec>" with latestSpec KEEPING the current pin; same-ABI chosen but lower-ABI skipped => note names the skipped build. RULING AMBIGUITY RESOLVED: bioconda rebuilds each interpreter at the SAME build_number so py310..py314 all tie at _1; the ruling says "highest build_number" but expects py310hf7cbfa5_1 — left to the old sort key the tie falls to the build STRING and picks py314h3004d98_1 (verified). Ruling: break ties by CLOSENESS to the currently pinned interpreter (yields the ruling's expected build, keeps the move a pure rebuild) — cost if wrong: pins stay on their current interpreter until a genuinely higher build number appears. Closeness is ONLY a tie-break (tested: higher build_number still wins; forward ABI move still taken).
C7/C8 fix round 1: pysam TAKEN py310hf7cbfa5_0 -> py310hf7cbfa5_1 (semantic diff: only packageSpec moved). Live: pysam 0.24.0 build _1 on Python 3.10.20, `import pysam` succeeds. Tier 1 GATE PASS 189/0/0; require-mode + verify root filter ONTGenotyping|NativeToolRunner|ToolVersionConformance = 80 executed/0 failures/0 SKIPS. Python suite 411 OK (9 new tests). SIDE EFFECT CHECKED not assumed: openpyxl/lofreq/flye flipped update->same; queried anaconda.org — each is ALREADY at the highest build number for its OWN interpreter (_3/_1/_16), so nothing is masked. BONUS: the rule also fixed a SECOND latent downgrade — whatshap 2.8's candidate moved py39h9f1bf47_0 -> py311h1457a19_0, matching its py311 pin (whatshap is a not-yet-taken version bump, so this would have bitten a later sweep).
C7/C8 fix round 1: FIXED A THIRD PRE-EXISTING STALE MIRROR (verified failing at HEAD by stashing, not caused by my change): test_bump.py test_main_supports_hold_only_and_retire asserted `--hold samtools` leaves 1.23.1, broken when C6 moved samtools to 1.24; now compares against the real manifest so a future re-pin cannot make it stale. Same class as C6's four. Remaining holds after the fix: sra-tools (build only, no py tag, out of scope), 3 no-arm64-build, 4 manual-check, ncbi-taxonomy (C5 ruling). NO tool is now held for a Python ABI regression. Open concern: check-upstream.py still ranks NON-python build strings lexically (didn't bite here; same bug class could recur for another convention).
C7/C8 fix round 1: 6645040a ABI-aware selection (Ruling: same-build-number ties break toward the interpreter closest to the current pin), pysam py310 _1 taken, doc SHA fixed, whatshap candidate corrected to py311, stale test_bump mirror fixed. Head 745c5332.
C7/C8: fix round 1/5 (4 addressed, 0 open; commits 31013572..745c5332). C5-C8 complete: 2026.2 manifest state = group1 (22 ids + Sparkle 2.9.6) + samtools/bcftools/htslib 1.24 + spades 4.3.0 + bbmap 40.02 + savont 0.6.3 + micromamba 2.9.0 + cutadapt/pysam newer builds. HELD: ncbi-taxonomy (zip), bracken/blast (no arm64), sra-tools build-only, gatk4/whatshap (experimental), viralrecon, EsViritu DB, deacon indexes.
Task C9: complete (commit c7deabf1). Goldens regenerated at 2026.2; diff vs 2026.1 = 11 same, 2 different. ACCEPTED: sarscov2-deacon `check_pairs` additive key (numerics identical). REJECTED AS NOT-A-TOOL-CHANGE: sarscov2-megahit 12->11 contigs — megahit pin did NOT move this sweep (bioconda::megahit=1.2.9=h96a01ab_8 identical to 2026.1); 13 isolated runs all give 12, the 11 appeared only in 2 full-batch runs; recorded the reproducible 12. KEY FINDING: kraken2-mini golden is BYTE-IDENTICAL despite the 20260626 index (rename verified present in inspect.txt: Betacoronavirus pandemicum taxid 3418604) because the fixture's 3 synthetic reads are ALL UNCLASSIFIED — the recipe cannot witness the rename, so the unchanged golden must NOT be read as evidence the new taxonomy was verified. Provenance: 12 sidecars written with no --overwrite needed (B8 guard never reached); audit passed. conformance dir = 252K (< 5MB) so 2026.1 kept.
Task C9: FIXED 2 GUARDS THAT WOULD HAVE GONE SILENTLY STALE: test_diff_goldens.py CURRENT_SET and test_fixture_provenance_scripts.py CONFORMANCE_GOLDEN_FIXTURES both hardcoded "2026.1"; now derived from the manifest / RETAINED_FIXTURES. Python 411 OK.
Task C9: OPEN ITEM (tier 2 gate reliability): sarscov2-megahit can fail tier 2 for reasons unrelated to any dependency change (2nd tier-2 run returned exit 2 on exactly this). 12 vs 11 = 8.3% on num_seqs and 5.7% on N50, both OUTSIDE the 5% tolerance; only sum_len (2.7%) stayed in. Candidates recorded in the conformance README: run megahit single-threaded, or compare only sum_len. Not taken here (changes a recipe, not a pin).
Task C10: complete (commit 99dd9c8e). swift package update moved 20 packages + added swift-configuration 1.2.0 transitively; ALL exact pins held (containerization 0.24.5, grpc 1.27.5, protobuf 1.35.0, Sparkle 2.9.6). Biggest: swift-log 1.9.1->1.15.0, swift-nio 2.94.0->2.101.3, async-http-client 1.30.3->1.36.0. check-package-resolved-consistency --repair PASS. Both products build clean. NOTE: `swift package update` ends with a line labelled `error:` about async-http-client disabling default traits on a package declaring none; it does NOT stop resolution and both products build — watch on a future SwiftPM upgrade.
Task C10: FIRST GATE RUN RED (c7deabf1): 12 XCTest failures / 7 tests, NONE environmental and NONE caused by C9/C10 (both commits touch zero Swift source). All 7 were STALE TEST MIRRORS of pins moved by groups 1-3, already failing at 745c5332 — this was the first full gate since those bumps. Fixed at 7e0ed286: AboutAcknowledgementsTests (nextflow/bcftools/spades/esviritu), ToolReferenceCommandTests (micromamba/nextflow/samtools), PluginPackRegistryTests (bbmap/pysam), WorkflowRegressionTests (micromamba x2), TaxTriagePipelineTests (taxtriage v3.3.6->v3.3.8 from releaseVersion). That makes NINE stale mirrors across the sweep: a pin bump is not done when manifest+live root agree, because mirrors only surface on a full gate.
Task C10: RULING (kept literals, did NOT derive): my first attempt read these from ManagedToolLock. VERIFIED WRONG by sabotaging the built resource bundle with a bogus bcftools version — the derived assertion STILL PASSED, because AboutAcknowledgements builds its detail string from the same manifest, so the test compares the manifest to itself. Literals are what catch an unintended pin move; refreshing them each sweep is the intended cost. Matches C6's precedent at aaf492fa.
Task C10: gate rerun at 7e0ed286 = 13433 executed, 36 skipped, 1 XCTest failure + swift-testing FileSystemWatcher suite (6 issues). BOTH environmental, both CONFIRMED by isolated rerun (SRASearchIntegrationTests 6/0; FileSystemWatcher 17/17 passed). Script prints GATE FAIL because it counts any failure; against the known baseline this run is green. Machine load avg ~30 from unrelated user apps throughout (same condition behind the megahit flip).
Task C9+C10: docs appended to docs/reports/2026-08-18-dependency-sweep-2026.2-results.md (commit e182aa3c, no em dashes). ~/.lungfish never touched; all swift invocations serialized.
Task C9: complete (c7deabf1 goldens 2026.2 + provenance; 7e0ed286 stale mirrors). Diff vs 2026.1: deacon check_pairs (accepted), megahit num_seqs 12→11 under load only (NOT a tool change; Ruling: recipe -t 1 + regenerate that golden; widen tolerance only if still nondeterministic). kraken2-mini golden cannot witness the taxonomy rename (3 synthetic unclassified reads) — Kraken2BrackenConformanceTests does via taxids.
Task C10: complete (99dd9c8e SwiftPM minor updates: 20 packages moved, exact pins held; gate 13433 executed, 1 failure = environmental). Note: swift package update prints a non-fatal error-labelled async-http-client traits diagnostic. Process note: run the gate per group next sweep so stale test mirrors surface next to the bump; mirrors comparing manifest-to-manifest are tautological (behavioral tests preferred).
Task C11: GUI walkthroughs still blocked (Computer Use); CI dispatch requires pushing the branch (ask user); release notes deps-2026.2.md to write now.
Task C11: complete (non-interactive parts). Commits 11cce28c (megahit -t 1) + 3c6859c2 (release notes + sweep checklist). MEGAHIT DETERMINISM CONFIRMED: goldens.json recipe now -t 1; THREE consecutive regenerations byte-identical (num_seqs 12, sum_len 3941, N50 332) AND identical to the committed -t 2 golden, so NO golden numbers moved and numericTolerance stays 0.05 (widening not needed). Only meta.json timestamp changed. Sidecar refreshed delete-then-write; purpose string + conformance README updated. diff_goldens --set 2026.2 = sarscov2-megahit SAME; provenance audit passed; python 411 OK. RULING UPHELD: AssemblyConformanceTests.testMegahitProducesFinalContigs KEEPS -t 2 — it asserts structure only (status==0, contigCount>0, no counts) and its cap documents the real MEGAHIT 1.2.9 arm64 >2-thread crash that AssemblyRunRequest.effectiveThreadCount mirrors in production; README now records the distinction so nobody "fixes" it later.
Task C11: docs/release-notes/deps-2026.2.md written (276 lines, no em dashes). Old->new tables DERIVED by diffing the parsed manifest at d0385ecf vs HEAD (not transcribed) = 31 entries moved; holds w/ reasons; behavior notes (build-string reinstalls, SARS-CoV-2 rename taxid 3418604, Bracken on Apple Silicon + D degrades, no Kraken2 special builds, bwa-mem2 self-reports 2.2.1); verification tier table w/ 4 open items; the 2026.1 plan verbatim; plus a DRAFT block for the next v0.5.0-betaNN.md (that file NOT created).
Task C11: 2026.1-MACHINE PLAN (read-only clone /tmp/lge-2026.1-clone, --plan only, clone verified unmodified then deleted; ~/.lungfish never written, still no receipt): 23 env reinstalls + 1 advisory DB (human-scrubber) + 1 bootstrap (micromamba 2.0.5->2.9.0-0), 3.62 GB. Breakdown: 18 specChanged, 4 buildChanged at UNCHANGED version (cutadapt, flye, lofreq, whatshap; pysam 5th), 1 metadataMismatch (bracken). KEY FINDING for the notes: flye/lofreq/whatshap appear in the PLAN but in NO manifest version table (build-only moves) — exactly the user-visible consequence a version table cannot express, hence the dedicated behavior section. APFS cp -Rc cloned 45G at zero extra space.
Task C11: docs/release/dependency-sweep.md updated w/ what THIS sweep proved missing: step 1 Python ABI direction (a "same" row may be an ABI-downgrade skip); step 3 risk-ordered groups via --only + semantic manifest diff + RUN THE GATE PER GROUP (9 stale mirrors found, 7 at the end belonging to 3 earlier groups) + keep mirrors as LITERALS (derived == manifest-vs-itself, proven by sabotage); step 4 --root always + --seed-from first run + read BOTH halves of swift test + gate log not console tail + PLAN GATE TOLERATES ADVISORY-ONLY DBs + confirm the pin actually moved before accepting a golden diff (make nondeterministic recipes single-threaded instead of re-recording) + sidecars delete-then-write + set-keyed guards must read the manifest; tier 3 --root + prereq check + blocked!=pass; step 7 derive the table + capture prev-set --plan against a clone; step 8 FIXED STALE CLAIM that verify.sh doesn't exist yet. Known risks 7/8/9 added: DB display names (+ can a golden even witness it), untrustworthy self-reported versions (review the exception list each sweep), bbtools JRE on a major bump.
Task C11: NOT DONE per brief (no push, no CI dispatch, no GUI walkthroughs/Computer Use, no subagents, no betaNN file). Swift NOT invoked at all by this task, so zero lock contention. Concerns: (1) megahit -t 1 proven on THIS host only, CI is independent confirmation; (2) notes ship with tier 3 / savont-on-real-ONT / GUI walkthroughs still open and say so explicitly; (3) the plan reflects ONE root with the full toolset — a user with fewer optional tools sees a shorter list.
Task C11: non-interactive parts complete (11cce28c megahit -t 1 deterministic; 3c6859c2 release notes deps-2026.2.md + sweep checklist). 2026.1-machine plan: 23 reinstalls + 1 advisory DB + 1 bootstrap, 3.62 GB. Open (user-gated): GUI walkthroughs (Computer Use), CI dispatch (push), savont real-data run; tier 3 (environment).
Task C12: final gate running (b0icvjhrm) on 3c6859c2; final Plan C review dispatched (fable) on final-review-code-only.diff (ca433879..3c6859c2).
Final review (fable): With fixes. Important: (1) ReconcilerServices.live.metagenomicsDatabaseVersions repeats the catalogID-nil miss → registerExisting rows never planned (GUI/CLI plan omits the Kraken2 index updates); (2) Plugin Manager has no Update action for a metagenomics DB (Remove only); (3) release-notes count prose contradicts the pasted plan (17 spec / 5 build / 1 metadata); (4) Kraken2 index downloads unverified (no S3 sidecars — controller probed: .md5/.sha256 404; ETag multipart) → Ruling: document; follow-up: multipart-ETag verification. Minors: silent nearest-kmer substitution message; preflight provenance lacks dialect; dialect cache per actor lifetime; verify.sh --dry-run before real-root guard; bump.py timestamp churn on no-change; golden count wording.
Ruling: single fix wave covers Important 1-3 + minors (dry-run guard order, bump.py no-change churn, preflight dialect provenance, substitution surfaced in result message, notes wording); Important 4 documented as follow-up.
Task C12: final gate (3c6859c2): XCTest 13433 executed, 36 skipped, 0 failures; swift-testing 6 = FileSystemWatcherTests (environmental) → GREEN modulo environmental. Fix wave dispatched (opus).
Final fix wave: complete (7101f8a3 reconciler catalogID resolution, caf5de66 GUI Update button, 52e5ed92 verify.sh/bump.py minors, c007f845 Bracken provenance + substitution, docs commit follows). Important 1: extracted MetagenomicsDatabaseInfo.resolvedCatalogEntry/resolvedCatalogID as the single expression of the two-step match (catalogID, else name+tool); ReconcilerServices.metagenomicsDatabaseVersions now keys by the resolved id, so registerExisting rows (Viral, Standard-16, catalogID==nil) are planned like catalog-installed ones. TIE-BREAK RULING: when two rows resolve to one catalog identity the row that RECORDS the identity wins, because that is the row resolveUpdateTarget will actually replace — cost if wrong: the plan would quote the wrong installed version for a duplicated database. Tests: ReconcilerMetagenomicsVersionMapTests (4), DependencyPlannerTests.testRegisterExistingDatabaseWithoutCatalogIDIsPlanned (uses the manifest's own kraken2-viral pin so it moves with the sweep), DbCommandUpdateTargetTests.testToolsUpdatePlanIncludesADatabaseRegisteredWithoutCatalogIdentity (seeded temp root, offline, asserts db list and tools update --plan --json agree).
Final fix wave: Important 2 — Plugin Manager Databases tab now has an Update button (Lungfish Orange, beside Remove, only when isUpdateAvailable), wired through PluginManagerViewModel.updateDatabase to updateDatabase(catalogID:) by the RESOLVED id, as an OperationCenter .download op (start/log/update/complete/fail), disabled while DependencyReconciliationActivity.shared.isApplying and while that row is downloading/removing/updating. updateNotSupported (locally built DBs) renders inline on the row as guidance, NOT via handleError, and completes the operation with a warning rather than failing it. The registry call is INJECTED via an updateDatabaseAction closure on init so the action is unit-testable offline (4 new tests in DatabasesTabTests + the existing view-source guard extended to the new button). GUI RENDERING NOT VERIFIED — Computer Use unavailable this session; walkthrough item added to the results doc's open list with 5 specific things to confirm.
Final fix wave: minors — verify.sh real-root guard moved ABOVE the --dry-run return (a rehearsal was blessing a plan the real run refuses; verified: --root ~/.lungfish --dry-run now exits 64, ordinary --dry-run still 0). bump.py no longer rewrites tool-versions.json/VERSIONS.txt/THIRD-PARTY-NOTICES when only the generation timestamp would move (compare with the stamp excluded; refresh_derived_files reports only what it wrote) + 2 tests. Bracken: both preflight sites now pass dialect:/effectiveArgv:; effectiveArgv is SHELL-QUOTED (a database path with a space previously read as two arguments, so the recorded command was not the command that ran); the nearest-read-length kmer_distrib substitution now rides the completed outcome's message and a kmerDistributionSubstitution provenance option instead of living only in os_log.
Final fix wave: docs — deps-2026.2.md counts corrected to 17 specChanged / 5 buildChanged (cutadapt, flye, lofreq, whatshap, pysam) / 1 metadataMismatch (counted from the pasted plan, not transcribed); golden counts phrased "12 recipes / 13 outputs" (verified against scripts/deps/goldens.json); added GUI+CLI instructions for taking a Kraken2 index update in both the notes body and the betaNN draft. Results doc gained a "Final fix wave" section incl. the Kraken2 INDEX INTEGRITY note: the S3 bucket publishes NO checksum sidecars (probed live this session: k2_viral_20260626.tar.gz.md5 and .sha256 both 404; ETag "df6f9dd95095a9ea2e0cfc580b0f684a-69" is a 69-part multipart digest, not an MD5 of the payload), so the manifest pins no digest and the installer validates structurally; follow-up = derive the part size and verify the multipart ETag.
Final fix wave: verification — swift build clean; swift test --skip-update --filter 'Dependency|PluginManager|Metagenomics|DbCommand|ToolsCommand|ClassificationPipeline|Bracken' = 423 executed, 0 failures; python3 -m unittest discover -s scripts/tests = 413 OK; bash -n scripts/deps/verify.sh OK. ~/.lungfish never touched. All swift invocations serialized (one at a time).
Fix wave: commits 3c6859c2..7bc90b1c (resolvedCatalogID shared resolver + planner fix; Plugin Manager Update button; verify.sh guard order; bump.py no-change churn; preflight dialect provenance + argv quoting + substitution surfaced; notes counts 17/5/1; integrity note). Re-review dispatched.
Fix wave re-review (opus): Approve. All prior findings addressed (Important 1-3, minors a-f, cited by file:line; shared resolver verified at all three call sites). NEW Important: resolveUpdateTarget step 2 iterated builtInCatalog sorted by catalogID while resolvedCatalogEntry takes builtInCatalog.first, so a display name mapping to two catalog entries could pick different survivors (unreachable today, but defeats the shared resolver). Fixed inline by controller (7cdbd160): step 2 now iterates hand-registered rows and resolves each via resolvedCatalogEntry, so the survivor is by construction the same. Minor: waitUntil now throws WaitTimeout after recording the failure (one failure, not two). Filtered gate 179/0/0. Full final gate on 7cdbd160 running.
```
