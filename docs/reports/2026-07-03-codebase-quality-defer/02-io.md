# LungfishIO — Deferred Items (Phase 2)

Module: `Sources/LungfishIO/**` (139 files, ~66K LOC).
Protocol: audit -> apply (behavior-preserving only) -> build + scoped tests ->
independent adversarial review -> revert-on-uncertainty -> commit.

IO is heavily correctness-sensitive (format parsing, coordinate math, bgzip
random access). The bar for "apply" is provably behavior-preserving:
dedup, dead-code removal, clarity renames of locals, access-control tightening.
Anything touching parsing/coordinate/format logic, a `Codable` schema, or a
failure path is DEFERRED here with a precise rationale, not applied.

Binding invariants preserved (never refactored away):
- bgzip `readUncompressedRange` infinite-loop fix: break when
  `nextOffset <= currentUncompressedOffset` OR `findBlock` returns nil, in BOTH
  the async and sync readers.
- Never save alignment as SAM (always sorted + indexed BAM).

## Deferred items

### Batch 1 — `NaoMgsDatabase.swift` (split + defer-reorder applied; rest deferred)

Applied: 4-way extension split + two `defer { sqlite3_finalize(stmt) }` reorderings
(behavior-identical, `finalize(nil)` no-op). Deferred (audit confirmed these are NOT
provably behavior-preserving):

- Duplicated 25-column `INSERT INTO virus_hits` VALUES list (createStreaming ~443 vs
  bulkInsertHits ~1469). Extractable to a shared constant only if byte-identical, but
  the two sites bind differently and one is `async static`; touches the insert path.
- Near-duplicated schema DDL across `createSchema`, `init(at:)` migration, and
  `openReadWrite`. Sites differ (`IF NOT EXISTS` vs `ALTER TABLE`, ordering interacts
  with migration detection). High blast radius.
- Repeated `String(cString: sqlite3_errmsg(db))` + throw (dozens of sites) — every one is
  an error/throw path (out of scope).
- `mergeStageSummaries` staged finalize-cascade — each guard finalizes a different growing
  subset; a helper would change error-path resource lifetime.
- `computeCoveredBasePairs` interval-merge, `normalizeImportedSampleName` regex,
  percent-identity/MAPQ/quality math, hash/dedup — parsing/coordinate/numeric logic.
- `NaoMgsTaxonSummaryRow` Codable + `topAccessions` JSON decode — schema/decode path.
- `populateSampleHitCounts` partial-failure control flow (`continue`/early-return fallback).

### Batch 2 — `AnnotationDatabase.swift` (dedup + split applied; rest deferred)

Applied: deduped the 4x-repeated 12-column row decoder into `decodeRecord`
(diff-verified all four original blocks were byte-equivalent modulo local var names
and a `bcType` temporary in two of them), dropped one orphan `// MARK: - Errors`,
5-way extension split. Deferred:

- WHERE/filter SQL assembly shared between `annotationTableQueryParts` and the inline
  `query`/`queryCount` builders — consolidating touches clause ordering + `ESCAPE '\'`.
- `toAnnotation` block/qualifier parsing and `transformed(...)` RC/clip/BED12 recompute
  (incl. the intentional trailing-comma difference vs `toBED12PlusLine`) — coordinate math.
- `append{Text,Numeric,Annotation}ColumnFilter` operator dispatch + empty-value edges — filter semantics.
- `createFromBED`/`createFromGFF3` bind blocks — NOT exact-equivalent (int32 vs int64 on col 8).

### Big-file audits with NO safe apply (recorded; deferred wholesale)

- `ONTGenotypeResultBundle.swift` (1495L): one dead-code item (unused `private struct
  OrderedSet`) — fold into a cluster batch. NO split (would force Codable-adjacent access
  widening of `SupportKey`/`CallSupportContext`/sort/parse helpers). Everything else is
  genotype/scoring/parse/Codable — deferred.
- `VariantDatabase+Bookmarks.swift` (1374L): MISNAMED — only ~150L are bookmark CRUD;
  ~1220L are the `createFromVCF` VCF->SQLite import pipeline. Only safe apply = remove a
  dead doc-comment (~L14). HIGH-VALUE DEFERRED STRUCTURAL ITEM: relocate the import
  cluster (152-1371) into `VariantDatabase+Import.swift` (its natural home — already
  references `allIndexStatements` here) or a new `+CreateFromVCF.swift`. No promotions
  needed (all `static`/`internal`/`public` at type scope), but it is cross-file and
  touches the correctness-sensitive VCF ingest path -> its own reviewed pass, not this
  behavior-preserving batch.
- `TwelveSAmpliconResultBundle.swift` (1321L): statement-level clean; all dedup
  candidates (`percentage`/`percent`, `nonEmpty`, `maxSamplePercent`, `totalExactReads`)
  are cross-type and/or scoring math -> deferred. Split (models vs +IO) is clean and
  needs no promotions — queued as a split-only batch.
- `GenBankReader.swift` (1134L): audited, monolithic parser logic, APPLY empty, NO split
  worth the churn (reader ~740L has no clean internal seam). No action.

### Batch 3 — `ProjectUniversalSearchIndex.swift` (2 micro-edits + split applied)

Applied: (1) deleted a redundant `normalizedInput` recompute in `organismAliases(for:)`
(reviewer confirmed `normalizedKey` is a pure function -> the two computations are
provably equal); (2) dropped a redundant `String(...)` wrap around a `lastPathComponent`
(already `String`); (3) 4-way extension split (+SQL/+Indexers/+Helpers). Many
`private`->`internal` promotions, all cross-file-necessary, none public; the 3 seed
structs stay nested in the class. Diff-verified pure relocation + the 2 edits.

### Mid-size cluster audits — recorded (mix of one-line applies + wholesale defers)

**4 sibling SQLite DB classes (TaxTriage/Nvd/EsViritu/Kraken2):**
- APPLY (queued into a cluster batch): `TaxTriageDatabase.escapedLikePattern` (~848-861)
  is byte-equivalent to the existing module helper `SQLiteLikePattern.contains`; its sole
  caller (~675) builds exactly `"%\(escapedLikePattern(text))%"`. Replace call with
  `SQLiteLikePattern.contains(text)` and delete the private helper. Grep-verified sole caller.
- APPLY (queued): remove `NvdDatabase.init(url:)` (~210) + its stale MARK — grep-verified
  ZERO callers (all construction goes through `init(at:)`; no `self.init(url:)`). Dead.
- DEFER (big structural, its own reviewed pass): these 4 classes are a copy-paste template
  (error enum, `xxBindText`, open/deinit/pragmas, `insertMetadata`/`fetchMetadata`/
  `fetchSamples`, `optional{Text,Int,Double,Bool}` helpers, repeated SELECT column lists).
  A shared `SQLiteReadableDatabase` base or `SQLiteStatement`/`SQLiteConnection` value type
  would remove hundreds of lines but crosses all 4 files and touches error-type identity +
  failure paths.
- DEFER: `NvdDatabase` two column-migration blocks (~127-193) -> `ensureColumn(...)` helper
  (manipulates live db handle across reopen with per-reopen failure paths); repeated
  26-column SELECT projection -> `hitColumns` constant (SQL text).

**Format parsers (GFF3/VCF/GTF/BED/FASTA/BgzipIndexedFASTA):** NO-OP for this pass — every
candidate is parsing/coordinate/format/loop-break logic. POSITIVE CONFIRMATIONS recorded:
- bgzip `readUncompressedRange` loop-break fix INTACT in BOTH readers of
  `BgzipIndexedFASTAReader.swift`: async findBlock-nil break (354-356) + nextOffset<=current
  break (373-375); sync findBlock-nil break (609-612) + nextOffset<=current break (630-633).
- No forbidden `Task { @MainActor` anywhere: every `Task {` (GFF3:169, VCF:352, GTF:188,
  BED:185, FASTA:104) is immediately followed by `do {`.

**Barcode-data + importers (Illumina/ONT/Fluidigm barcode tables, ONTDirectoryImporter,
NaoMgsResultParser):**
- DEFER (borderline dead-data): `ONTBarcodeData.nativeBarcoding24FASTA` (~27-79) is a
  grep-verified zero-reference `internal static let`. Removing it IS behavior-preserving,
  but it is a documented vendor barcode table (NB01-NB24, SQK-NBD114-24 etc.) that the
  maintainer may intentionally retain for future kit support. Low value to delete, real
  risk of removing deliberately-kept reference data -> DEFERRED per revert-on-uncertainty.
  Downstream can delete if confirmed unwanted.
- DEFER (structural split seam, flagged not applied): `ONTDirectoryImporter.swift` (888L)
  has a clean non-data seam — header parser (`ONTReadMetadata`/`ONTReadHeaderParser`),
  model types (`ONTImportConfig`/`ONTImportResult`/`ONTImportError`/layout), and the
  self-contained `decompressGzipPrefix` gzip util — each extractable to its own file.
  Deferred: the bodies touch scan/import/concurrency; a `git mv` split is worth its own pass.
- DEFER: `ONTImportError.readCountFailed` case is defined + `errorDescription`-handled but
  never thrown (grep-confirmed). It is a `public enum` case, so removal is source-breaking
  API — API-cleanup pass, not this one.
- Barcode SEQUENCE data across all kits: untouchable even where bytes overlap (distinct
  vendor kits). `ColumnMap` / `nao*Field` free funcs correctly `internal` (used by
  `NaoMgsDatabase+Create/+Summaries`), not tightenable.

### Batch 4 — `GenotypeHaplotypeAnalysis.swift` (split + 1 micro-edit)

Applied: 4-way split by top-level type (+Matcher/+LocusResolver/+Analyzer; models group
stays in the original filename), zero access promotions (all top-level types, same module).
One verified edit: `if uppercased == "AG" || uppercased.hasPrefix("AG")` ->
`if uppercased.hasPrefix("AG")` (the `== "AG"` disjunct is fully subsumed). Diff-verified
this is the ONLY content change. Left untouched (per audit): the two distinct
`primaryAllelesForDominance` (instance property vs static, compute different things) and the
two byte-identical private run-number helpers (folding them is a deferred cross-type change).

### Registry / Services / Compression audit — recorded

- APPLY (queued into a cluster batch): remove dead `public enum FormatRegistryError`
  (FormatRegistry.swift ~766-795) — grep-verified ZERO references in Sources/ + Tests/;
  the registry throws `ImportError`/`ExportError`, never this enum. (Caveat: it is a
  `public` type; removing public API is only safe because no in-repo consumer references it.
  Downstream should confirm no out-of-tree consumer before merge.)
- DEFER (split candidate): `FormatRegistry.swift` (795L) — extract
  `createBuiltInDescriptors()` (~354-762, ~410 lines of declarative data) to a sibling
  `FormatRegistry+BuiltInDescriptors.swift`, dropping the actor file to ~385L. Cross-file
  move + access change on a static factory feeding the actor init -> its own pass.
- DEFER: `FormatRegistry` init/register map-building dedup (different: init builds fresh
  dicts, register mutates state); `detectFormat` ordering (format-detection logic).
- RULE-CHECK PASSES (recorded for the deliverable):
  - `NaoMgsBamMaterializer.swift`: streams SAM into `samtools view -bS - | sort -o *.bam`,
    NEVER writes a `.sam` file. No SAM-save. (BAM output + `.bai` index.)
  - `MarkdupService.swift`: BAM-in / BAM-out (`sort -n | fixmate -m | sort | markdup`),
    atomic replace + index. No SAM-save.
  - `GzipSupport.swift`: no `readUncompressedRange` offset loop (shells to `gzip -dc`, which
    handles concatenated BGZF members); all chunk/line `break` conditions correct, preserved.
  - No forbidden `Task { @MainActor` in any audited Registry/Services/Compression file.
- DEFER (cross-file): `SQLITE_TRANSIENT_DESTRUCTOR` redefined in NaoMgsBamMaterializer,
  KrakenIndexDatabase, ProjectStore, BundleBrowserMirrorStore -> a shared `LungfishCore`
  constant. The three gzip-process scaffolds in GzipSupport and the samtools process-run
  scaffolds in MarkdupService differ per-body (stdout capture, error mapping) -> not
  exact-equivalent, deferred.

### Mid-size Bundles cluster audit — recorded (0 applies)

All 7 files (ReferenceBundle, MSAActionRegistry, AlignmentDataProvider,
GenotypeAnnotationSidecar, AlignmentMetadataDatabase, HaplotypeDefinitionStore,
GenotypeCohortSmartFilter) are already clean: no dead private code, no safe intra-file
dedup that doesn't cross a coordinate/SQL/Codable/failure-path boundary. No SAM writes.
Deferred API-surface items (removing `public` symbols is not provably behavior-preserving):
- `HaplotypeDefinitionStore.provenanceSuffix` (~20) — grep-verified unreferenced `public
  static let`, but public API; confirm intent before removal.
- `ReferenceBundle.getAnnotationsFromDatabase` (~432) — no external callers; could
  internalize if the `limit:` API isn't a promised contract.

### Later big-file audits (batches queued, not yet applied)

Recorded here so nothing is lost if a batch is deferred for time:

- `ProjectUniversalSearchIndex.swift` (2484L): APPLY = dup `normalized` recompute
  (~1784), redundant `String(...)` wrap (~540); DEFER = all query/rank/ordering/ID/parse
  logic; split candidate (SQL layer / indexers / helpers) needs private->internal promotions.
- `GenotypeHaplotypeAnalysis.swift` (1967L): APPLY = redundant `== "AG"` disjunct (~351);
  DEFER = all thresholds/scoring/calling/coordinate math/Codable; split by type (same
  module, no promotion needed) optionally folding two dup private helpers.
- `MultipleSequenceAlignmentBundle.swift` (1941L): APPLY = dedup 4x annotation-sort
  closure, tighten `ColumnStat` (internal); DEFER = coordinate maps, parsers, Codable
  schema, SQLite persistence, and the pre-existing `manual-manual-` ID quirk (do NOT
  "fix" — it changes a persisted identifier).
- `PhylogeneticTreeBundle.swift` (1809L): APPLY = inline one redundant local (~115);
  DEFER = all Newick emission/escape dedup (byte-identity != test-verified; feeds
  checksummed provenance), non-identical checksum helpers, parsing, topology math;
  split candidate needs private->internal promotions.
- `AnnotationDatabase.swift` (1719L): APPLY = dedup the 4x 12-column row-decoder into one
  helper (high value), drop stray `// MARK: - Errors`, cosmetic `= nil` locals; DEFER =
  WHERE/filter SQL assembly, `toAnnotation`/`transformed` coordinate math, int-vs-int64
  bind differences in the INSERT paths; split candidate.
- `FASTQDerivatives.swift` (1672L): APPLY = collapse always-`"cutadapt"` `toolLabel`
  (~829, display-only); DEFER = all orient-map/trim/demux read-selection, atomic-write
  helpers, trim-coordinate parsing, materialization-state mapping; split candidate needs
  NO promotions (all types already public).
