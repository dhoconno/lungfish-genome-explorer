# Lungfish v0.4.0-alpha.3 Release Notes

Debug prerelease build for export and sidebar fixes.

## Changes

- Show single and batch sequence exports in the Operations Panel with progress, cancellation, and CLI-equivalent convert commands where available.
- Resolve nested analysis runs inside user grouping folders in the left sidebar/sidecar.
- Replace the BAM primer removal scheme chooser with a standard macOS picker-style control.
- Update fallback release smoke checks for the current SwiftPM resource bundle layout and scrub Release app executables for local path leaks.

Validation included focused Swift tests, primer trim and Operations Panel XCUI
tests, real GenBank export smoke artifacts, release smoke checks, and
Release/Debug app builds.
