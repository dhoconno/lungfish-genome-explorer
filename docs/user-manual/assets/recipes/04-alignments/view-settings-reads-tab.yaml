# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: the Inspector View Settings, Reads tab, with chr20 reference bundle selected
id: view-settings-reads-tab
chapter: 04-alignments/02-reading-an-alignment
caption: The Reads tab of the Inspector's View Settings, showing the row controls, the Maximum displayed
  depth control, and the base and strand toggles beneath them.
viewport_class: alignment
app_state:
  fixture: '{demo_project}'
  open_files:
  - reference: Reference Sequences/chr20_10.0-10.5Mb.lungfishref
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Clicked the View tab in the Inspector
  # PROSE-ONLY: Clicked the Reads sub-tab under View Settings
crop:
  mode: region
  region:
  - 1112
  - 455
  - 288
  - 425
post:
  retina: true
  format: png
