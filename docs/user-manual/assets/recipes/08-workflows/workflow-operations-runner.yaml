# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Demo project, Imports/HG002 selected, Tools > Genotyping > miSeq amplicon MHC genotyping...; the Workflow Operations window at the top of its settings (Overview, Inputs with the Project Reference picker, Primary Settings), with the workflow list at the left including a linked Hello World Nextflow package marked Enable in Library.
id: workflow-operations-runner
chapter: 08-workflows/03-running-external-workflows
caption: "The Workflow Operations window opened from a Tools category submenu, with the enabled workflows listed down the left, a linked package marked Enable in Library, and the Overview, Inputs, and Primary Settings sections on the right."
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select Imports/HG002 in the sidebar
  # PROSE-ONLY: Choose Tools > Genotyping > miSeq amplicon MHC genotyping...
  # PROSE-ONLY: Cancel without running
crop:
  mode: window
post:
  retina: true
  format: png
