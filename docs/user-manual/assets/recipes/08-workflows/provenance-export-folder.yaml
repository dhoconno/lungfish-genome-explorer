# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Exported by Preview build 4673 from chr20 reference import lineage.
id: provenance-export-folder
chapter: 08-workflows/02-exporting-as-nextflow-or-snakemake
caption: The exported provenance folder open in Finder, with the primary artifact beside the provenance
  subdirectory of copied run records.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Finder
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Show export in Finder. Use List view, Hide Sidebar, resize to 900 by 380 points, and capture all four top-level entries.
crop:
  mode: window
post:
  retina: true
  format: png
