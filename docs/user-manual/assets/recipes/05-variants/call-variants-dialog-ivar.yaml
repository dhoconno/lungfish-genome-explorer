# Captured from Lungfish Preview 2026.9.41 with capture-window.sh (2026-09-24 manual review).
# App state: Demo project, chr20 bundle, Tools > Call Variants..., iVar selected on the untrimmed HG002 minimap2 track: the primer-trim confirmation is unticked, the iVar Options section (0.75, 0.25, 20, Ignore strand bias on) shows, and Run is unavailable until the BAM is confirmed as primer-trimmed.
id: call-variants-dialog-ivar
chapter: 05-variants/01-calling-variants-from-amplicons
caption: "The Call Variants dialog with iVar selected on an alignment that has not been primer-trimmed, showing the unticked primer-trim confirmation, the iVar Options section that only iVar displays, and the Readiness line asking you to confirm the BAM was primer-trimmed."
viewport_class: variant
app_state:
  fixture: '{demo_project}'
  open_files:
  - reference: Reference Sequences/chr20_10.0-10.5Mb.lungfishref
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Same dialog, clicked iVar in the tool sidebar
  # PROSE-ONLY: Ticked 'This BAM has already been primer-trimmed for iVar' (this track was not actually primer-trimmed by LGE, so the checkbox is not auto-derived/greyed as it would be on a real primer-trimmed track)
crop:
  mode: region
  region:
  - 208
  - 102
  - 982
  - 696
post:
  retina: true
  format: png
