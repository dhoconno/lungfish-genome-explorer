# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Same dialog as genotyping-run-dialog, scrolled a little to show the Haplotyping group: Analysis Mode segmented picker on Genotyping only, beside Deterministic haplotyping, with the caption 'Report detected alleles and supporting reads without assigning haplotypes.'
id: genotyping-analysis-mode
chapter: 09-genotyping/02-running-genotyping
caption: "The dialog's Haplotyping group with the Analysis Mode segmented picker set to Genotyping only, beside Deterministic haplotyping, and the caption describing the chosen mode."
viewport_class: none
app_state:
  fixture: '{williams_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Same Workflow Operations dialog as genotyping-run-dialog, no AI provider configured.
  # PROSE-ONLY: Scroll the dialog to bring the Haplotyping group into view.
  # PROSE-ONLY: Crop to the Haplotyping heading, the Analysis Mode segmented control, and its caption line.
crop:
  mode: region
  region:
  - 285
  - 505
  - 590
  - 85
post:
  retina: true
  format: png
