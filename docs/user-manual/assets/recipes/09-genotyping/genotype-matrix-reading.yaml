# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: In the Williams copy project, clicked Analyses/Amplicon genotyping results/amplicon-genotyping_3. The genotype matrix shows allele-target rows (Genotype column pinned at left) and one column per sample, with the Inspector Bundle tab showing Run Summary, Excel and QC Status.
id: genotype-matrix-reading
chapter: 09-genotyping/03-reading-the-genotype-comparison
caption: The genotype result window on the Williams MiSeq result, with allele-target rows named by their
  reference record down the pinned left columns and one column per sample across the top.
viewport_class: genotype
app_state:
  fixture: '{williams_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: In the sidebar, expand Analyses > Amplicon genotyping results.
  # PROSE-ONLY: Click amplicon-genotyping_3 and press Return to open the genotype matrix.
  # PROSE-ONLY: Capture the full window showing the pinned left columns and the sample columns scrolling to the right.
crop:
  mode: window
post:
  retina: true
  format: png
