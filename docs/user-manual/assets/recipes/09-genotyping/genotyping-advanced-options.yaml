# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Demo project, Imports/HG002 selected, Tools > Genotyping > miSeq amplicon MHC genotyping..., scrolled down with Advanced Options expanded: Analysis Mode on Genotyping only, the minimap2 arguments field, Keep Intermediates, and the Output Directory group (Analyses/Amplicon genotyping results).
id: genotyping-advanced-options
chapter: 09-genotyping/02-running-genotyping
caption: The dialog's Advanced Options disclosure expanded on the miSeq workflow, showing the minimap2
  arguments field and the Keep Intermediates checkbox above the Directory group.
viewport_class: none
app_state:
  fixture: '{williams_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Same Workflow Operations dialog as genotyping-run-dialog.
  # PROSE-ONLY: Click the small chevron beside the Advanced Options label (below the Advanced Settings heading) to expand it.
  # PROSE-ONLY: Scroll to bring minimap2 arguments and Keep Intermediates into view, capture the full window, then click Cancel.
crop:
  mode: window
post:
  retina: true
  format: png
