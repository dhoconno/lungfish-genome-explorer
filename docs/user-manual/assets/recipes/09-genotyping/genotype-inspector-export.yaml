# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Williams copy project, amplicon-genotyping_3 selected; the Inspector Bundle tab scrolled to the Excel section holding the Export to Excel... button.
id: genotype-inspector-export
chapter: 09-genotyping/04-haplotype-definitions-and-export
caption: "The Excel section of the Inspector's Bundle tab on a genotype result, holding the Export to Excel… button."
viewport_class: genotype
app_state:
  fixture: '{williams_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: With the Genotype Display section open in the Inspector's View tab, scroll to its foot.
  # PROSE-ONLY: Crop to the Export heading, the Filtered Pivot... button, and its caption text, without clicking Filtered Pivot...
crop:
  mode: region
  region:
  - 1085
  - 555
  - 312
  - 62
post:
  retina: true
  format: png
