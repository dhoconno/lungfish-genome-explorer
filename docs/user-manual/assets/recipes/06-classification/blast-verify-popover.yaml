# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# Cropped to the viewport columns because the Inspector showed stale NVD Result content.
# App state: Verify via NCBI BLAST popover open over the Riboviria taxon row in the Kraken2 taxonomy viewport, showing the Reads to submit slider, the NCBI warning, and the Run BLAST button (not clicked)
id: blast-verify-popover
chapter: 06-classification/06-blast-verification
caption: "The Verify via NCBI BLAST popover open over the sunburst, showing the Reads to submit slider with its typed number field, the warning that reads leave the app for NCBI, and the Run BLAST button."
viewport_class: taxonomy
app_state:
  fixture: '{demo_project}'
  open_files:
  - analysis: kraken2-SRR36291587
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select kraken2-SRR36291587 in the sidebar
  # PROSE-ONLY: Select the Riboviria taxon row
  # PROSE-ONLY: Click BLAST Verify
crop:
  mode: region
  region:
  - 0
  - 0
  - 1059
  - 900
post:
  retina: true
  format: png
