# Captured from the Lungfish Debug build of 2026.9.43 (2026-09 campaign), whose storage root ~/.lungfish-debug has no EsViritu database. Region crop in window points.
# App state: Database section reads Database not installed with the Download Database... button
id: esviritu-database-missing
chapter: 06-classification/03-running-esviritu
caption: The dialog's Database section reading Database not installed, with the Download Database... button
  beside it.
viewport_class: none
app_state:
  fixture: Debug build storage root ~/.lungfish-debug with no EsViritu database, small SARS-CoV-2 test
    bundle
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Debug
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Use a storage root with no EsViritu database installed, such as the Debug build's ~/.lungfish-debug
  # PROSE-ONLY: Select a small SARS-CoV-2 FASTQ bundle in the sidebar
  # PROSE-ONLY: Tools > Classification > EsViritu...
crop:
  mode: region
  region:
  - 296
  - 236
  - 690
  - 64
post:
  retina: true
  format: png
