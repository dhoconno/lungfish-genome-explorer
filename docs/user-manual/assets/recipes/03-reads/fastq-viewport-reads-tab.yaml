# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# Shared with 01-foundations; same capture, copied for this part.
# App state: Clicked Imports/HG002 (paired, 91,148 reads, defaults) and chose the Reads tab of the FASTQ viewport, listing the first records with columns #, Read ID, Length, Mean Q and Sequence.
id: fastq-viewport-reads-tab
chapter: 03-reads/03-quality-control
caption: The Reads tab of the FASTQ viewport, listing the first records with their read identifier, length,
  mean quality, and sequence.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Click the HG002 FASTQ bundle under Imports in the sidebar
  # PROSE-ONLY: Click the Reads tab under the sparkline strip
crop:
  mode: window
post:
  retina: true
  format: png
