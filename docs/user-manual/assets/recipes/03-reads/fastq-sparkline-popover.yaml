# Captured from Lungfish Preview 2026.9.41 with capture-window.sh (2026-09-24 manual review).
# App state: Demo project, Imports/HG002 (paired, 91,148 reads) selected, Q / Position sparkline clicked open: the Per-Position Quality Scores panel fits its width and groups the 250 positions into two-position boxes (1-2 ... 249-250).
id: fastq-sparkline-popover
chapter: 03-reads/03-quality-control
caption: The Q / Position sparkline clicked open into its full-size popover, showing per-position quality
  falling away over the last few bases of the read.
viewport_class: fastq
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected Imports/HG002 in the sidebar.
  # PROSE-ONLY: Clicked the Reads tab.
  # PROSE-ONLY: Clicked the Q / Position sparkline to expand the Per-Position Quality Scores panel.
crop:
  mode: region
  region:
  - 425
  - 212
  - 540
  - 350
post:
  retina: true
  format: png
