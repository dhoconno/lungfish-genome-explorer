# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Selected Imports/HG002 in the sidebar. Chose Tools > Decontamination > Remove Human Reads... The pane has no database row; it uses the managed Deacon pan-human index.
id: human-scrub-dialog
chapter: 03-reads/05-decontamination
caption: "The Remove Human Reads pane, with only the selected read bundle in the Inputs section and a line of text in place of settings controls, because the operation always uses the managed human index."
viewport_class: fastq
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected Imports/HG002 in the sidebar.
  # PROSE-ONLY: Chose Tools > Decontamination > Remove Human Reads...
crop:
  mode: region
  region:
  - 211
  - 100
  - 974
  - 699
post:
  retina: true
  format: png
