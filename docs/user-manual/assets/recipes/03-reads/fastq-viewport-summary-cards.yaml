# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# Shared with 01-foundations; same capture, copied for this part.
# App state: Clicked Imports/HG002 (paired R1+R2 imported with defaults, 91,148 reads, no binning). The FASTQ viewport shows the nine summary cards above the three sparkline charts, Operations tab selected.
id: fastq-viewport-summary-cards
chapter: 03-reads/03-quality-control
caption: The FASTQ viewport for the HG002 chromosome 20 slice, showing the nine summary cards above the
  three sparkline charts.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Click the HG002 FASTQ bundle under Imports in the sidebar
crop:
  mode: window
post:
  retina: true
  format: png
