# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The chr20_10.0-10.5Mb bundle at full width with the Variants tab of the table drawer open, showing the genome track, the HG002 track summary, the Depth coverage, and the variant rows table with bcftools rows (Filter column reading a bare dot).
id: variants-tab-hg002-bcftools
chapter: 01-foundations/05-variants-and-vcf
caption: "The HG002 bcftools variant track as a table, one row per VCF row, with the Chrom, Position, Ref, Alt, Quality, and Filter columns taken straight from the file."
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Click the chr20_10.0-10.5Mb reference bundle under Reference Sequences
  # PROSE-ONLY: Open the sequence record into the viewport
  # PROSE-ONLY: Click the Variants tab in the table drawer
  # PROSE-ONLY: Choose Sequence > Go to Location... and enter chr20_10.0-10.5Mb:1-500001 to show the whole bundle
crop:
  mode: window
post:
  retina: true
  format: png
