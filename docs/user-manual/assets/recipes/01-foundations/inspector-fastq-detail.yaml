# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Demo project open, Imports > HG002 (paired, 91,148 reads) selected, Inspector on the Bundle tab with Ingestion expanded (Clumpified, Compressed, Pairing Interleaved, original R1 and R2 file names, sizes).
id: inspector-fastq-detail
chapter: 01-foundations/06-the-lungfish-project
caption: "The Inspector in close-up for the same FASTQ selection, showing read counts, length and quality statistics, the Ingestion group with the pairing and the two original file names, and the start of the editable metadata fields."
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq_bundle: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Click the HG002 row under Imports in the sidebar
  # PROSE-ONLY: Click the Ingestion disclosure triangle in the Inspector
crop:
  mode: region
  region:
  - 1061
  - 51
  - 339
  - 849
post:
  retina: true
  format: png
