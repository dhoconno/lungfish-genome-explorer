# Captured from Lungfish Preview 2026.9.40 with capture-window.sh (2026-09-24 manual review).
# App state: Demo project open, Imports > HG002 (paired, 91,148 reads) selected in the sidebar, the FASTQ operations view in the viewport, the Inspector on the Bundle tab showing Dataset Statistics and Sample Metadata.
id: inspector-fastq-selected
chapter: 01-foundations/06-the-lungfish-project
caption: The demo project with a paired-end FASTQ bundle selected in the sidebar, the FASTQ operations
  view filling the viewport, and the Inspector showing dataset statistics and sample metadata.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq_bundle: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Click the HG002 row under Imports in the sidebar
crop:
  mode: window
post:
  retina: true
  format: png
