# SDD ledger — plan: docs/superpowers/plans/2026-08-29-release-process-hardening.md

## Pre-flight consistency scan

| Scope | Producer / consumer relationship | Finding / ruling |
|---|---|---|
| Task 1 internal | JSON contract → strict loader → builder channel query tests | Consistent; behavioral output is the contract boundary. |
| Task 2 internal | Contract/toolchain policy → Doctor; pinned Package.resolved → Sparkle resolver | Consistent; package mode excludes credentials, credentials mode extends it. |
| Task 3 internal | Deterministic scratch token → scanner allowlist; unsigned payload → receipt | Consistent; allowlist is exact and receipt binds transformed binaries. |
| Task 4 internal | Doctor → output preparation → package → receipt → sign/resume | Consistent; destructive preparation is after Doctor and receipt precedes signing. |
| Task 5 internal | Package receipt → tag push → exact-SHA CI → sign/publish | Consistent; CI cannot publish and coordinator owns ordering. |
| Task 6 internal | Contract semantics → validator/docs; process evidence → smallest test stabilization | Consistent; no production timeout behavior changes without reproduction. |
| Task 7 internal | Full tests + real unsigned dual-channel package + Sol review | Consistent; signing/publication are explicitly excluded. |
| Tasks 1 & 2 | Task 1 contract is consumed by Doctor and resolver in Task 2 | Interface names and exact toolchain bounds agree. |
| Tasks 1 & 4 | Builder query introduced in Task 1 is expanded into phase behavior in Task 4 | Shared builder edit is sequential; Task 4 preserves contract-derived values. |
| Tasks 1 & 6 | Contract semantic values are consumed by validator/docs in Task 6 | Coexistence and shared bundle identifier agree with Global Constraints. |
| Tasks 2 & 4 | Doctor and Sparkle resolver are invoked by phased builder | Task 4 must not duplicate preflight logic; consume CLI reports. |
| Tasks 2 & 5 | Coordinator invokes Doctor and supplies local credential profile | Tracked coordinator/wrapper must not reintroduce machine constants. |
| Tasks 3 & 4 | Scanner and receipt CLIs are consumed by package/resume phases | Exact scratch path and candidate app path are shared; schemas are fixed in Task 3. |
| Tasks 3 & 7 | Receipt/scanner evidence is rechecked in whole-branch package verification | Consistent; final verification uses real produced candidates. |
| Tasks 4 & 5 | Builder phases are orchestrated by the common coordinator and CI | Coordinator must call interfaces, not copy assembly/signing logic. |
| Tasks 4 & 6 | Builder usage/docs and compatibility flag migration are reconciled | Docs must name receipt resume, not raw path reuse. |
| Tasks 4 & 7 | Package-only dual-channel candidate is the real final build gate | Consistent; no credentials or remote side effects. |
| Tasks 5 & 6 | Coordinator/CI ordering is encoded in semantic authority validation | Consistent; exact-SHA CI precedes publication everywhere. |
| Tasks 5 & 7 | CI/coordinator tests and real package run prove final ordering and packaging | Consistent; network publication is stubbed/forbidden during verification. |
| Tasks 6 & 7 | Stabilized process test and semantic validator are rerun in final gate | Consistent; no task depends on an unverified process-test change. |

Ruling: Preserve `com.lungfish.browser` for both channels while enforcing distinct app filenames, names, feeds, and updater hosts — changing the identifier would strand existing Preview Sparkle users — cost if wrong: Launch Services/defaults/TCC may remain shared even though both app paths coexist.

Ruling: Support Xcode 26.4.1 through the end of major version 26 and Swift 6.2 through the end of major version 6 instead of an exact patch pin — both CI 26.4.1 and the release Mac 26.6/Swift 6.3.3 must be valid — cost if wrong: a future compatible-looking patch may require an explicit deny/pin after regression evidence.

Ruling: Set the release-build free-space floor to 20 GiB — recent archive, SwiftPM, DMG, and dual-channel package work need substantial transient storage — cost if wrong: smaller release Macs will be rejected until the threshold is deliberately lowered with evidence.

Ruling: Permit package-only to apply only literal-identity `-`, timestamp-free ad-hoc seals to Mach-O payloads after path sanitization and before exact-payload executable smoke — arm64 macOS kills a transformed executable with either an invalid or removed signature, while these identity-free seals use no private credential and are replaced by Developer ID only after receipt verification — cost if wrong: CI candidates may be mistaken for distribution-signed artifacts, so documentation, tests, and metadata must continue to distinguish ad-hoc sealing from Developer ID signing.

Ruling: Validate the exact scratch, release, archive, and DerivedData targets through one shared read-only boundary before Doctor may create a probe or the builder may clean/build — recognized ignored repository defaults remain supported while aliases, symlinks, overlaps, repository escapes, and unrelated external outputs fail closed — cost if wrong: older unmarked custom output directories require an explicit safe migration instead of silent reuse.

Ruling: Preserve the receipt-bound unsigned candidate and apply Developer ID signatures only to a private disposable copy after receipt verification — a separately named signed output is retained only after successful app notarization — cost if wrong: release staging uses one additional app-sized copy, but any signing/notarization/DMG failure remains resumable without repackaging.

Task 1: fix round 1/5 (3 addressed, 0 open; commits 77f2254..1852f56)
Task 1: minor (deferred): implementer report overstates the ordering of Stable bridge rejection relative to the current credential guard; Task 4 will move credentials into the later phase.
Task 1: complete (commits 189aec5..1852f56, review clean)
Task 2: fix round 1/5 (4 Important and 1 Minor addressed; commits 5b87883..3a7d603)
Task 2: fix round 2/5 (1 Critical cache-provenance finding addressed; commit dcd1429)
Task 2: fix round 3/5 (1 Important ancestor-rename finding addressed; commit 177e397)
Task 2: complete (commits 5b87883..177e397, 48 focused and 75 broader tests green, independent Sol review clean)
Task 3: fix round 1/5 (5 Important and 1 Minor scanner/receipt findings addressed; commit f088d637)
Task 3: fix round 2/5 (2 Important filesystem-alias/dynamic-fallback findings addressed; commit 818ab6c)
Task 3: fix round 3/5 (1 Important exact-string-boundary finding addressed; commit 3cda13c)
Task 3: complete (commits eef1499..3cda13c, 34 focused and 114 broader tests green, independent Sol review clean)
Ruling: Permit identity-free ad-hoc seals on transformed Mach-O files during package-only assembly so macOS can execute the exact payload before receipt creation — this does not access a Developer ID or Keychain and the receipt binds the sealed bytes — cost if wrong: the candidate is distribution-unsigned but not literally signature-free, and Developer ID signing replaces those local seals later.
Task 4: fix round 1/5 (1 Critical and 3 Important target/retry/tool-path findings addressed; commit 95d7a91)
Task 4: fix round 2/5 (1 Critical archive-ownership and 1 Important late-DMG retry finding addressed; commit 5d1e2ad)
Task 4: fix round 3/5 (1 Critical relocated-receipt cleanup finding addressed; commit a28a366)
Task 4: complete (commits 7417540..a28a366, 69 focused and 174 broader tests green, real dual-channel package evidence at 7417540, independent Sol review clean; final current-HEAD real rerun deferred to Task 7)
Task 4: fix round 1/5 (4 independent-review findings addressed; 62 focused and 166 broader release Python tests green; real dual-channel package rerun deferred to Task 7 by ruling)
Task 4: fix round 2/5 (1 Critical archive-ownership and 1 Important late-retry finding addressed; 66 focused and 171 broader release Python tests green; real dual-channel package rerun remains deferred to Task 7)
Task 4: fix round 3/5 (1 Critical relocated-receipt deletion finding addressed; 69 focused and 174 broader release Python tests green; real dual-channel package rerun remains deferred to Task 7)
Task 5: fix round 1/5 (2 Critical, 4 Important, and 2 Minor independent-review findings addressed; immutable recovery and exact remote verification added; final verification recorded in task-5-report.md)
Task 5: fix round 2/5 (4 Important and 1 Minor independent-review findings addressed; complete-vs-partial transaction classification, canonical signed ancestors/hashes, selected-remote GitHub binding, and detach-safe recovery added; 117 focused and 202 broader release tests green; final evidence appended to task-5-report.md)
Task 5: fix round 3/5 (1 Critical and 3 Important independent-review findings addressed; github.com host binding, effective fetch/push identity, receipt-bound mutable completion evidence, and selected-repository Sparkle URLs added; 30 final focused and 193 broader release tests green; final evidence appended to task-5-report.md)
Task 5: fix round 4/5 (3 Important independent-review findings addressed; exact canonical receipt verification before tagged-state classification, contract-exact appcast evidence, and lowercase ASCII GitHub identity across package recovery added; final evidence appended to task-5-report.md)
Task 5: complete (commits a22e82f..0f38a81, 124 focused and 196 broader release tests green in the final round, independent Sol review clean)
Ruling: Resolve Xcode once in the parent and export its canonical `DEVELOPER_DIR` to Doctor, Sparkle resolution, gates, builder, candidate receipt, and nightly children — Doctor-local selection cannot repair an ambient Command Line Tools environment for later siblings — cost if wrong: a caller with multiple full Xcodes must set `DEVELOPER_DIR` explicitly rather than relying on a non-default ambient selection.
Ruling: Use `build/Release/Lungfish.xcarchive` inside the marked release output and `.build/release-derived-data` as the coordinator/CI defaults, with CI logs outside `build/Release` — these are the exact repository paths accepted by `release_target_security` — cost if wrong: CI loses per-channel custom output directories, but matrix jobs have isolated checkouts and retain receipts, metadata, and logs.
Ruling: Credentials Doctor runs before compilation or tag/remote mutation and again after exact-SHA CI immediately before signing; live Sparkle validation belongs to the common coordinator and is repeated on receipt resume — package-only stays credential-free but can no longer be promoted through the coordinator without current credential and feed evidence — cost if wrong: a long source/CI gate may require the release operator to unlock credentials twice.
Ruling: Packaging requires non-shallow history on the configured main branch with selected remote main proven as an ancestor of the local release-preparation commit; Stable uses Preview as a strict build-number migration floor and allows 404 only for its own uninitialized feed — exact equality with remote main would incorrectly reject the intentional unpushed release commit — cost if wrong: remote-main ancestry depends on the earlier fetch retaining that remote commit locally.
Task 5: P0 wiring repair complete (shared Xcode export, target-valid defaults, coordinator live-feed/history gates, double credential Doctor, and nightly ordering implemented; 172 exact focused and 200 release-focused discovery tests green; release skill validator, Python compile, shell syntax, and CI YAML parse green; Swift package compiled before 12 pre-existing hardening-branch static assertion issues)
Ruling: Direct builder access is limited to unsigned package-only and contract queries; every credentialed prepare, resume, recovery, signing, notarization, or publication request requires a fresh capability passed only to the builder child by `release.py`, and resume repeats the full source-history gate — cost if wrong: intentional low-level recovery must enter through the coordinator rather than invoking the phase script manually.
Ruling: Contract-derived live floors are Alpha and Beta strictly for Preview, and Alpha plus Beta strictly followed by Stable with 404 allowed only for the Stable feed — omitting the legacy Alpha contract path is fatal — cost if wrong: an unavailable legacy migration feed blocks release instead of silently risking a lower build number.
Ruling: Receipt-bound floors are rechecked after exact-SHA CI immediately before the credentialed child, immediately before the first Developer ID signature, and after signing/notarization before any publication — cost if wrong: these repeated live reads add release latency; a narrow response-to-command race remains until publication is split behind a server-side compare-and-swap/conditional asset update.
Task 5: P0 wiring independent-review repair complete (direct credentialed bypass, omitted Stable Alpha floor, and post-CI/signing-interval feed races addressed; 196 exact focused and 207 release-focused discovery tests green, plus 51/51 coordinator tests after the final child-boundary recheck; release validator, Black, Python compile, shell syntax, CI YAML parse, and diff check green)
Ruling: Model Debug under a separate strict `buildProfiles` namespace rather than adding a third release channel — Preview and Stable keep their existing release identities and updater paths, while Debug has exact local identity and false release/publication/updater capabilities with no place for feed, public-key, GitHub, tag, DMG, or publication metadata — cost if wrong: any future publishable channel still requires a deliberate contract schema and release-pipeline change instead of promotion from Debug.
Ruling: Retire the public release mode in `scripts/build-app.sh`, resolve the supported Xcode through the shared selector, and allow only explicit local ad-hoc signing — distribution packaging remains reachable solely through the release package interface — cost if wrong: developers using the old shortcut receive a hard migration error instead of an approximate release app.
Core follow-on Task 8: complete pending commit (37 focused Python contract/preflight/Sparkle tests and targeted Swift build-configuration tests green; Xcode/Swift policy remains Xcode >=26.4.1,<27 and Swift >=6.2,<7, with exact receipt versions unchanged)
Ruling: Derive every shared default state namespace from the exact runtime app identity: Debug uses `~/.config/lungfish-debug`, `~/.lungfish-debug`, `Library/Application Support/Lungfish Debug`, identity-specific logs, recipes, workbook attestations, caches, temporary roots, and `com.lungfish.secrets.debug`, while Preview and Stable retain their historical paths and all injected overrides remain authoritative — cost if wrong: any newly introduced persistent/cache/temp default must also consume `LungfishAppIdentity` or risk crossing profiles.
Ruling: Package runtime resource bundles only under `Contents/Resources` and route owned production resources through `RuntimeResourceLocator`; do not create wrapper-root compatibility symlinks for generated SwiftPM accessors because they invalidate the app's code seal — cost if wrong: new direct production `Bundle.module` access must be migrated or the moved Debug smoke will fail rather than silently falling back to the compiling checkout.
Core follow-on Task 9: complete pending commit (identity/default-state isolation tests, focused portable-resource tests, three shell-smoke fixture tests, real ad-hoc-signed Debug build, and relocation/resource probe with the compiling `.build` hidden green; no user UI or production state opened)
Ruling: A Debug relocation smoke must treat the compiling `.build` as shared mutable repository state: lock it exclusively, restore only into an absent destination, preserve both trees on conflict, and expose the hidden original as the recovery path — cost if wrong: a concurrent compiler can make the smoke fail safely, but its newly created `.build` is never overwritten.
Ruling: App production resource lookup validates the case-exact in-bundle `Contents/Resources` boundary before resolving symlinks, then requires canonical containment for every candidate; source fallback is available only through explicit test injection — cost if wrong: a standalone development CLI must package resources or report them missing instead of reading the checkout implicitly.
Ruling: Debug's Nextflow home and cached TaxTriage checkout use `Library/Caches/com.lungfish.debug/nextflow`, while Preview and Stable keep `~/.nextflow`; user metadata presets and the legacy database root use identity-aware managed storage — cost if wrong: external scripts that assumed Debug shared Stable's caches must migrate rather than silently reading production state.
Audit: remaining production literals matching `.lungfish`, `.nextflow`, `Application Support/Lungfish`, `Library/Logs/Lungfish`, or `com.lungfish.secrets` are bundle/project file-format markers, run-local logs/staging names, a container configuration example, or explicit legacy Conda migration recognition. None is an unresolved app-wide default state root.
Core follow-on Task 10: independent-review correction in progress (4 Important findings reproduced; adversarial smoke, resource containment, complete state-default, and stale-skill validation RED tests added before implementation).
Core follow-on Task 10: fix round 1/5 complete pending commit (21 Python smoke/skill tests, 10 RuntimeResourceLocator tests, focused identity/state/main-probe Swift tests, validator/syntax/diff checks, and a current-source real ad-hoc Debug build plus locked relocation smoke all green; `.build` restored with no hidden tree or lock retained).
Ruling: Public production resource lookup outside an exact app wrapper exposes no executable-adjacent SwiftPM bundle or checkout roots; only explicit developer/test injection may add them. An app's raw `Contents/Resources` node must not be a symlink, and its canonical target and every child must remain beneath the canonical app wrapper — cost if wrong: standalone CLI layouts must package resources inside an app or opt into an explicit development mechanism instead of discovering `.build` artifacts implicitly.
Ruling: Debug relocation verifies the identity of every signed nested code object/container discoverable beneath the wrapper, not only deep seal validity and the three top-level targets; each must report `Signature=adhoc` and `TeamIdentifier=not set` — cost if wrong: probing every bundle node adds smoke latency, but a nested distribution identity cannot hide behind a valid mixed deep signature.
Core follow-on Task 10: fix round 2/5 complete pending commit (exact adjacent `.build` bundle, symlinked Resources root, nested Developer-like identity, and semantic case-insensitive skill contradiction adversaries reproduced RED; 24 Python smoke/skill tests, 12 RuntimeResourceLocator tests, validator/syntax/diff checks, and a current-source real ad-hoc Debug build plus locked relocation smoke green; `.build` restored with no hidden tree or lock retained).
Ruling: The real relocation smoke invokes literal `/usr/bin/codesign` beneath a minimal clean environment and exposes no verifier override; tests inject captured output only into a pure identity parser — cost if wrong: signature-verifier experiments must be performed outside the production smoke rather than through an environment hook.
Ruling: Debug documentation authority is one exact ordered facts block shared by the skill and catalog, plus an exact allowlist of operational command/reference lines. Any new Debug claim, including an accurate paraphrase, fails closed until the canonical validator contract is deliberately updated — cost if wrong: prose flexibility is traded for reviewable authority changes and deterministic contradiction rejection.
Core follow-on Task 10: fix round 3/5 complete pending commit (unsafe codesign override, absent pure parser, six regex-evasive contradictions, an unrecognized accurate paraphrase, and reordered facts reproduced RED; 27 focused Python tests, release-skill quick validation, repository validator, shell/Python syntax and diff checks, and a real canonical-codesign locked relocation smoke green; `.build` restored with no hidden tree or lock retained).
Ruling: Validator authority spans the complete normalized `## Debug build` section through the next same-level heading (or EOF for the catalog), not merely the facts sub-block or lines containing a keyword. Any added, removed, or reordered content line requires a deliberate validator-contract update — cost if wrong: even accurate editorial changes are blocked until both canonical authorities are reviewed together.
Core follow-on Task 10: fix round 4/5 complete pending commit (six post-facts contradictory claims plus missing, reordered, extra accurate, and catalog-only lines reproduced RED; 20 focused release-skill validator tests green).
