# Native final integration delta review

## Verdict and scope

**Approved for the bounded native integration delta. No new load-bearing defect found.** Reviewed `7fd2906..236e74af6b186367fd66f15e73b558a0c3e44a74` using the supplied delta package, current frozen matrix11 source and the existing scoped reviews. The frozen worktree was clean and at the stated endpoint. The earlier `native-final-review.md` covers base→7fd2906 and remains applicable except for explicitly versioned additions reviewed here.

This is cross-module integration approval, not a new MHC coverage/optimality, full11 feasibility, laboratory amplification, throughput or memory-bound claim. The full11 discovery was still live and was neither accessed nor changed. No broad suite or scientific experiment was run for this review. Root separately reports the frozen matrix11 full suite: **865 passed in190s**; that count was not independently rerun here.

The reviewer authored portions of history v2, clone/cache work, scheduling, secondary certificates, diagnostic compression and effort options. Approval of those implementations rests on the existing independent root/Astra scoped reviews listed below, **not self-approval in this report**. This review traces their integrated callers, identity boundaries and persisted outputs. No native code, managed runtime, live output/database, root progress ledger or public report was modified.

## Load-bearing integration checks

### Scientific identity, coverage and certificates

- The equal-distinct-observed-class, exact same-row forward/reverse support and primer-trimmed observed-position union machinery is unchanged. Terminal mismatch permissions only classify predicted specificity products; newly permitted intended/secondary witnesses explicitly carry zero additional coverage credit. Their endpoints do not become exact supported products. Duplicate aliases remain labels/multiplicity, not extra objective weight.
- The selected intended and secondary policies flow from CLI/resolved options into `AlleleConstraintProfile`; full profile identity participates in the oracle cache context. Primary intended and secondary controls remain independent, and old missing fields normalize to their historical exact defaults. A selector/work option does not implicitly change either policy.
- Concrete secondary screening remains pair-local and owner-specific: same target and observed row, two complete selected configurations, all four concrete projected footprints with actual terminal support, external witness oligo/orientation/full endpoints, ordered disjoint internal partners and unchanged product-distance bound. Shared physical sequences do not substitute an occurrence from another owner, target or shifted repeat. Unknown/missing/ambiguous footprints do not receive certificates. Existing exact certificates are retained first.
- Publication performs fresh selected-stage validation. Re-audit reconstructs science from authoritative stored raw targets and literal selected artifacts, compares saved intended/secondary certificate collections and counts, and checks the normalized profile. Bundle audit also binds policy declarations across config, options and provenance. Updating a stored certificate or profile and its superficial file hash does not bypass fresh reconstruction.
- Strict dimer rejection remains **score≤−26**, with the exact native bidirectional bounded-offset scoring contract. Pool exposure still counts unique physical sequences/edges, including self edges. Optional salvage retains cumulative edge and incident-species limits and cannot gain exact coverage from specificity certificates. This delta does not relax chemistry or dimer defaults.

### Search caches, registration and progress

- Proposal registration rejects immutable configuration-ID collisions before exposing a configuration to compatibility/search. The public search path registers generated configurations before querying them; it does not depend on retrying a previously queried unknown ID after registration.
- The per-state gain LRU caches only the unchanged gain expression, clears after every successful add/drop and does not cache target fractions. New immutable registrations leave existing contributions unchanged; eviction recomputes. Within-fill consumed-candidate tracking is local to an add-only fill, and repair/new fills start with a fresh set. It does not permanently reject a candidate after a pool/state-changing repair.
- Diagnostic compression is private to each immutable catalog/profile/stage oracle. Reasons remain an immutable tuple for boolean fast paths; diagnostic returns unpack fresh Python objects with the original shapes. Payloads are created from trusted process-local results only, never loaded from artifacts. Score-cache sharing across tiers keys intrinsic numerical measurements by sequence pair and kernel; acceptance/exposure still uses the current stage policy. Fresh publication/audit does not consume optimizer diagnostic verdicts.
- Serial remains the default scheduler. Reserved scheduling changes only cooperative phase service; the global deadline/cancellation and retained incumbent take priority. Full-cloud restrictions and salvage baseline validation remain connected. Phase metadata distinguishes work caps, exhaustion and interruption; objective before/after and accepted-repair deltas allow actual phase benefit to be assessed.
- Progress uses existing clock samples and provisional oracle-checked results. It persists selected configuration definitions, catalog digest and stage policy so generated incumbent IDs can be resolved after failure. It neither declares an intermediate panel valid nor replaces fresh publication. Observer and disk-write cost remain charged to wall time; only equivalent completed fixed work is promised to preserve decisions/history, not identical outcomes under an arbitrary tight deadline. Cleanup preserves the primary failure and records secondary close errors.

### Effort/options and compatibility

- `standard-v1` preserves the five historical compute defaults; explicit `quality-v1` changes only its versioned time/start/repair/attempt/family defaults. The actual request mask allows individual explicit values, including values equal to old defaults, to win. Saved resolved snapshots bypass effort recomputation. Scheduling, scientific preset/policies, salvage time, distinct-class weighting and discovery remain independent.
- Additive capability descriptors return explicit policy/effort identities. Legacy/non-allele paths retain old defaults and reject explicit allele-only controls. The old selected-output path does not need newly added request fields to be readable.
- Native labels are a separate display-only sidecar. Reuse regenerates labels from current stored raw headers while retaining scientific catalog identities. Audit uses already authoritative targets plus canonical observations, avoiding another full catalog load solely for labels. Advertised maps must exactly match fresh raw reconstruction and an inventoried output; historical absence remains allowed. Duplicate labels/order and immutable row/class IDs remain distinct. No label field enters cache/scientific digests.

### History and immutable discovery origins

- New SQLite v2 stores integer relations while preserving canonical payload bytes/IDs/order and public canonical-ID query views. Existing v1 is detected before DDL and remains read/append in place; no migration is attempted. Inspection and cache loading call the common format validator, rather than assuming only v1.
- Reference insertion row counts prevent silent missing relations. Entity-key caches are per-instance and bounded, cleared on rollback/close. Durable prefix/snapshot/reload validation still checks canonical records and references; incomplete durable tails are not represented as complete stages. FULL synchronous, DELETE journaling,1000-record batching and64MiB page cache remain explicit.
- Cache export/reuse still requires closed source history and complete immutable discovery identity. Copy-on-write clones/fallback copies are independent files, never hard links, and copied bytes remain hash-verified. The current run receives fresh selection history plus a separately labeled copied origin database; old selection dispositions cannot become current selection verdicts. Bounded query limitations remain disclosed rather than implying whole-database integrity validation.
- Verified with `git diff --name-only`: **all11 discovery-fingerprint dependencies have zero changes from both7fd2906 and frozen7ca64e6 to236e74a**. Selection policies, storage layout, labels, work effort and diagnostics therefore do not invalidate scientifically compatible cold discovery solely due to unrelated implementation edits. Source/runtime start/end receipts still gate each actual workflow.

## Evidence relied upon

Independent scoped reviews, rather than repeating their suites:

- `task-history-integer-v2-review-final.md`:97b1996; v1/v2 reload/append/query/cache/rollback/integrity contracts.
- `task-cache-clone-review-final.md`:5a652b9; independent clone/copy mutation/deletion and retained hash guards.
- `task-selector-replay-review-final.md`: consumed-candidate fix and fixed-work behavior.
- `task-phase-scheduling-review-final.md`:e105af4+1217c5c; global/local deadlines, cancellation, observability and ordinary-selector compatibility.
- `task-intended-site-policy-code-review.md` and intended-policy reports:92ea291; optional designated intended sites, exact coverage separation and old-output defaults.
- `task-concrete-secondary-policy-review-final.md`:95c5d00 integrated61d6ac8; four-site certificate proof, fresh audit and policy-specific identities.
- `task-search-progress-review-final.md`:2b18b7b+8d16837; resolvable provisional records, charged time and primary-error preservation.
- `task-gain-memoization-review-final.md`:20fe136; immutable registration/add/drop/eviction and complete fixed-work history parity.
- `task-diagnostic-cache-compression-review-final.md`:01cafef; isolation and scientific/history/counter parity against the raw-deepcopy adapter.
- `task-search-effort-native-review.md`:158a4a3; requested masks, saved snapshots, capability identity and explicit-controls parity.
- `task-allele-label-map-review.md`:115878f+236e74a; raw binding, tamper, reuse/relocation and no full-catalog label-only read.

No additional targeted falsification was warranted: inspection did not identify a concrete unresolved failure hypothesis beyond those already covered by the scoped regression evidence. Operational limits remain: large witness/oracle collections can still consume substantial memory despite compression; cooperative kernels can exceed a phase slice; heuristic search is not exhaustive; specificity is confined to supplied-row backgrounds. These are not new defects or empirical guarantees introduced by this approval.
