# Independent fixed-panel background screen review

## Initial package

External `fixed-panel-background-screen-v1/background_screen.py` SHA256 `6b8f3a075458c3ee0b3ef0ea199292228efa9df5f3923077cd69a59b0541fa81`; tests `91804bc464cb6a02508ca4979696505b5f83544d5538276d0c3dd579b659c2c4`. Independently rehashed both and ran frozen matrix07 `.venv/bin/python -m pytest test_background_screen.py -q`: **10 passed in0.75s**. Read implementation, tests and implementation report. No real screen or production edit.

## Scientific assessment

No blocking scientific defect found. The explicit partial context avoids a fabricated complete catalog; canonical raw parsing and content/occurrence identity preserve the matched A1 target, with source-index metadata remapped explicitly. Only selected site/family/configuration records are retained. Fresh projected exact products and selected-site metadata must match saved validation, then intrinsic and same-pool pair contexts use the original canonical selected-sites-v2 checker. Full baseline support and allowed witness multisets must match the original fresh validation. Expanded A1 witnesses are required to match baseline exactly, and other-target exemptions are forbidden. Unknown/IUPAC/full-extension uncertainty is handled by the actual kernel rather than a simplified sequence matcher. Same-pool filtering, shared species ownership, missing seeds and cross-row separation have focused tests.

All context results are retained as gzip JSONL before isolation checks. Summary failure leaves complete=false; a single native product tuple can exceed the cooperative time bound or memory expectation, which the report states honestly. No product truncation or altered predicate was introduced. Exact A1 coverage is descriptive only; the output does not claim reoptimized or all-target coverage. Candidate/background labels are bound to ordered raw rows and saved target IDs.

## Bounded fixes requested before execution approval

### P2: wrapper success must require its binding evidence

`verified_panel` makes wrapper artifact binding conditional on the presence of `outputs`, so a status-only wrapper receipt is accepted. For this specifically bounded completed07 diagnostic, require a nonempty output inventory covering native/audit receipt and audit validation, plus true `identityUnchanged` and `nativeIdentityUnchanged`. The real completed07 receipt contains these fields; read-only inspection confirmed that fact. Add missing-inventory/false-identity negatives. Native/audit artifact checks otherwise correctly bind all consumed selected files.

### P2: preserve a receipt for an existing-output failure

`execute` creates the output directory before establishing its receipt envelope. An existing output raises directly without workflow provenance. Keep that directory immutable and create a fresh deterministic sibling failure directory, recording requested versus actual output and failure status, consistent with the approved availability diagnostic. Add a regression checking original bytes unchanged and sibling receipt retained.

Initial verdict: scientific implementation approved in principle; final real-run gate held for these two provenance fixes. No production or scientific-predicate change requested.

## Final rereview — approved

Approved script SHA256 `af76991b9e3fbcff3f2a2a241d1672d371665a9ce8626ff7c792cc47c55b7e2b` and tests `e84f022fa5c342ce0a5347771d06eebcb23479d9064d5d592009b53d109593ee`. Independently rehashed both and reran the frozen matrix07 focused suite: **11 passed in0.75s**, exit0.

Both provenance findings are resolved. Wrapper success now requires a nonempty artifact inventory and true wrapper/native identity-stability flags before mandatory receipt/output byte bindings. Existing outputs are preserved and produce fresh sibling failure receipts with requested and resolved output, source/runtime and error evidence; the new test verifies repeated failure siblings and original byte preservation. Scientific logic is unchanged from the initial review.

No remaining blocking finding. Approved for parent-authorized freezing/execution of the fixed completed07 A1 versus original11 background component screen. No real screen was performed during review. Cooperative timeout and single-context tuple memory limits remain explicitly documented; an incomplete/failing run must not be interpreted as a whole-panel conclusion.
